# PvZ-C 功能缺口盘点（对照 README 逐项核实）

> 生成：2026-09-16 · 基准文件：`pvz.c`（6341 行）、`README.md`（477 行）、`RELICS.md`、`PROGRESS.md`、`VISUAL_UPGRADE_PLAN.md`
> 方法：README 每一条声称 → 到 `pvz.c` 找定义/引用点 → 到 `assets/` 找资源 → 判定「已完成 / 部分完成 / 未做 / 文档失真」

## 一句话结论

**README 里描述的东西，绝大部分已经做完了，而且不少地方是超额实现的**（40 植物 / 13 僵尸 / 76 遗物 / 6 关 / 6 模式 / 粒子系统，资源 165 张 BMP 全齐）。
真正剩下的缺口是三类：**① 3 个"代码写了但没接上线"的功能 bug；② 一批 P1/P2 视觉升级没做；③ 一批文档/注释已经和代码对不上了。**

---

## A. 真功能缺陷（代码存在但玩家永远看不到 / 触发不了）

### A1. 🔴 僵尸出场池被硬编码成只有 3 种 —— 9 种僵尸完全不可达（CRITICAL）

**证据**（`pvz.c:2956-2961`）：

```c
int pool[3], pn = 0, pick, type;
pool[pn++] = ZT_NORMAL;
if (idx >= 2) pool[pn++] = ZT_CONE;
if (idx >= 5) pool[pn++] = ZT_BUCKET;
pick = pool[rndi(0, pn)];
```

而紧邻的 2938-2947 行**明明给 9 种僵尸赋了 `zCost`**：

| 僵尸 | 解锁波次 | 设计意图（代码注释原文） |
|---|---|---|
| ZT_DANCER 舞王 | idx ≥ 5 | 召唤伴舞 |
| ZT_BALLOON 气球 | idx ≥ 8 | 无视地面防线 |
| ZT_ZAMBONI 冰车 | idx ≥ 10 | 碾压植物 |
| ZT_VAULTER 撑杆 | idx ≥ 4 | 克制前排坚果 |
| ZT_NEWSPAPER 读报 | idx ≥ 6 | 打断慢磨节奏 |
| ZT_SCREENDOOR 铁门 | idx ≥ 8 | 逼你上磁力菇/爆炸 |
| ZT_DIGGER 挖掘 | idx ≥ 11 | 逼你守后排 |
| ZT_FOOTBALL 橄榄球 | idx ≥ 13 | 逼你堆真实 DPS |
| ZT_GIANT 巨人 BOSS | idx ≥ 16 | 关底 BOSS |

**调用链唯一性已确认**：`spawnZombie()` 全文件只在 `pvz.c:3294` 被调用（从 `spawnQ[]` 取）；`spawnZombieAt()` 只在 `2839`（`spawnZombie` 内部）和 `4157`（读报僵尸死亡后原地生成 `ZT_NORMAL`）被调用。**`pool` 是僵尸种类的唯一来源**。

**后果**：
- README 第 260-274 行整节「13 种僵尸 —— 第二批 6 种是反制型」的描述**在实机中 0% 可验证**。
- `spawnZombieAt()` 中按 `switch(type)` 写的每种僵尸专属初始化（撑杆的持杆状态、铁门的门板 HP、挖掘的钻地计时、巨人的砸击冷却等），以及 `updateZombie()` 里对应的行为分支，**在实机中永远不会被进入**。
- 难度设计意图（"每种都在玩家刚形成某个套路的时候登场"）完全落空，实际只有"普通/路障/铁桶"三档线性加压。

**修法**（一行级）：把 `pool` 扩到 `ZT_COUNT`，改成"按 `zCost[type] != 0` 收集"即可自动跟随 2938-2947 的解锁表：

```c
int pool[ZT_COUNT], pn = 0, pick, type;
for (i = 0; i < ZT_COUNT; i++) if (zCost[i] > 0) pool[pn++] = i;
```

---

### A2. 🟠 两个遗物是死链 —— 抽到了也完全不生效（HIGH）

Python 全量扫描 `R_` 枚举 76 项在 `pvz.c` 中的引用次数，结果：**74 项各有 1 处 `relicsRecalc()` 里的 `HAS()` 钩子（效果已接），2 项引用数为 0**：

| 遗物 | 定义行 | 声明效果 | 实际 |
|---|---|---|---|
| `R_FURY` | `pvz.c:674` | 愤怒：愤怒条涨得更快 + 可主动引爆 | **0 引用** |
| `R_LOADOUT_PLUS` | `pvz.c:693` | 扩容卡组：出战卡槽 +2（可带 10 张） | **0 引用** |

注：愤怒条本身是常开的（`3176-3177` `gFury += dt` 满 45 触发雷击），与 `R_FURY` 无关——所以这个遗物抽到后**字面上什么都不做**，是纯粹的"假遗物"。`RELICS.md` 和抽卡池都把它当正常遗物在发。

**修法**：`R_FURY` 在 `relicsRecalc()` 里加 `if (HAS(R_FURY)) gFuryRate *= 1.5f;`（并接主动引爆按键）；`R_LOADOUT_PLUS` 里加 `if (HAS(R_LOADOUT_PLUS)) gCardSlots += 2;`（注意 859 行 `gCandidateSlots=5` 的上限联动）。

---

### A3. 🟡 关卡级种植列限制从未接入（MEDIUM）

**证据**：`LevelDef` 定义了 `colFrom, colTo`（`pvz.c:306`，注释"可种植的列范围"）与 `zwStart, zwEnd`（`311`），6 个关卡都填了值（如后院泳池填 `3, 9`，配 desc"只能种在右侧的泥地上"）。

但全文件 grep：`colFrom`/`colTo` **各只出现 1 次（就是定义那行）**，`zwStart`/`zwEnd` 同理。而 `cellLegal()`（`1478`）里只有硬编码的 `MODE_WATER` 规则：

```c
if (gCurMode == MODE_WATER && c < COLS - 3) return 0;   /* 水中：仅后 3 列 */
```

**后果**：
- 选「后院·泳池」关卡时，文字说"只能种在右侧泥地"，**实际全草坪都能种**（只有切到"泳池"模式才受限）。
- 「屋顶·天台」「沙漠·遗迹」没有任何专属种植限制，`lawnVariant` 只换了地面贴图。
- 关卡表里的 `zwStart/zwEnd`（僵尸种类解锁波次区间）是**双份死字段**——因为 A1 已经把出怪锁死成 3 种了。

**修法**：`cellLegal()` 里加 `if (c < levelDefs[gCurLevel].colFrom || c > levelDefs[gCurLevel].colTo) return 0;`（注意与 `MODE_WATER` 规则取交集而非叠加）。

---

### A4. 🔴 3 货币 / 2 能量的 HUD 是死代码 —— 玩家看不到自己有多少钱（HIGH）

**证据**：右下角状态面板整块（`pvz.c:5581-5647`，26 个 `putTextS`/`fillRound`/`spriteBlit` 调用）**画在 `gFrameDC` 上，但位于 5724 行的 `StretchBlt` 之前**。`StretchBlt` 会把 `gWorldDC` 整幅盖到 `gFrameDC` 上，因此这一整块**帧帧被擦除，从未出现在任何一帧里**。

被擦掉的是这 6 类状态量的**唯一**显示处：

| 状态量 | 变量 | 别处有显示吗 |
|---|---|---|
| 模式名 | `gCurMode` | 无 |
| 武器名 | `gCurWpn` | 无 |
| 金气 | `gGold` | 无（G 键召唤南瓜要花 10） |
| 幽能 | `gGhost` | 无（H 键全屏减速要花 20） |
| 充能 | `gCharge` | 无（`R_CHARGED` 遗物相关） |
| 愤怒条 | `gFury` / `gFuryMax` | 无（满 45 触发雷击） |

独立验证脚本 `_ui_proof_report.txt` 的 A 表已经把这块坐实：`pvz.c:5593 / 5634 / 5644` 三处调用在**全部 26 帧**（覆盖 13 个界面）里"全不可见"；B 节统计"从未可见的调用点：14 处"，其中 3 处属于这个面板。

**后果**：`G`/`H` 两个按键（`6010`/`6014`）的花钱逻辑存在、货币也确实在累积，但玩家**从头到尾不知道余额**，等于功能性 UI 缺失。

**修法**：把这 26 个调用整体搬到 5724 行之后（或搬到 5822 附近的文字层绘制段）。注意其中 `spriteBlit(gFrameDC, &gSprGold, ...)` 即便搬到最后也**不能用**——`spriteBlit` 内部按"逻辑坐标 × SS"算目标矩形，画在 1× 的 `gFrameDC` 上坐标会翻倍跑出画面，需要换成直接 `AlphaBlend` 或改画到 `gWorldDC`。

---

## B. 视觉升级未做（`VISUAL_UPGRADE_PLAN.md` 的 P1/P2 项）

以下符号在 `pvz.c` 中**出现次数为 0**，即计划里提到的效果一个都没做：

| 计划项 | 状态 | 现状 |
|---|---|---|
| 僵尸逐帧行走动画（`walkFrame/frameAnim/legFrame`） | ❌ 未做 | 僵尸只有 sin 波整体摇摆；`assets/` 里每种僵尸只有 1 张行走图 + 1 张倒地帧 |
| 卡片木纹 / 纸质感（`wood`） | ❌ 未做 | 卡片仍是纯色圆角矩形 |
| 卡面渐变（`gradient`） | ❌ 未做 | 无渐变填充 |
| 后处理暗角（`vignette`） | ❌ 未做 | 无全屏暗角 |
| 子弹拖尾（`trail`） | ❌ 未做 | 子弹是纯精灵位移 |
| 卡片翻牌动画（`flipAnim`） | ❌ 未做 | 三选一/通关奖励直接出卡，无翻牌 |

注：`VISUAL_UPGRADE_PLAN.md` 里"33 植物精灵缺失 / 10 僵尸缺失 / 需新建 particle.c"等结论**已经全部过期**——`assets/` 现有 **165 张 BMP**，40 株植物 + 13 种僵尸（含 eat/倒地帧）+ 23 种子弹 + 5 武器 + 6 模式图标**全部齐备**；粒子系统也已在 `pvz.c` 内联实现（`Particle` 结构体 `1137`、`addParticle()` `1602`、30+ 发射点）。

---

## C. 文档失真（需同步更新，非代码问题）

| 位置 | 文档写的 | 代码实际 | 差 |
|---|---|---|---|
| README:43 | 遗物 **44 项** | `R_COUNT` = **76**（`pvz.c:644-696`，与 RELICS.md 一致） | 少报 32 |
| README:113 | 存档 **264 字节** | 实际 `pvz_save.dat` = **572 字节**（`SAVE_VER=4`，含等级/经验/纪录/每日营地） | 少报 308 |
| README:117 / 223 / 226 / 236 | 编组「**12 株**」、一局「**8 张**」卡槽 | 普通植物 **32 株**（`i < PT_HERO_FLAME`）；实战卡槽 `gCardSlots=4`、候选位 `gCandidateSlots=5` | 数字全错 |
| README:321 | `assets/` **39 张** BMP | **165 张** BMP | 少报 126 |
| README:433 | 约 **1900 行** | **6341 行** | 少报 3.3× |
| README:260-274 | 13 种僵尸、第二批 6 种反制型 | 枚举/精灵/AI 都有，但**出场池只有 3 种**（见 A1） | 形同未实装 |
| `pvz.c:5289` 注释 | 「12 株植物里选 8 张带上场」 | 实际 32 株普通植物可选 | 陈旧注释 |
| `PROGRESS.md` | 粒子系统 `particle.h`/`particle.c` 完整实现；有 `_test_particle.c`、12 张测试 JPG、`test_plants/`、`generate_all_plants.sh` | 文件**确实存在，但已废弃、被挪进 `_scratch/`**（`particle.c` 9245B、`particle.h` 2059B、`_test_particle.c`、`generate_all_plants.sh`、`test_plants/` 下 12 张 JPG 全在）。**该独立模块路线后来被放弃**——粒子系统最终以 `Particle` 结构体（`pvz.c:1137`）+ `addParticle()`（`1602`）的形式**内联进 pvz.c**。资源侧也不是"缺失"，`assets/` 已有 165 张 BMP | 路线已废弃 + 进度百分比过期 |
| `VISUAL_UPGRADE_PLAN.md` | 107 张图、33 植物精灵缺失、10 僵尸缺失、待建 particle.c/material.c/animation.c/postprocess.c 共 5 个新模块 | `art/raw/` 确为 107 张源图（数字没错），但**下游资源已全部产出完毕**（`assets/` 165 张 BMP，40 植物 + 13 僵尸 + 23 子弹 + 5 武器 + 6 模式图标齐全）；5 个新 .c 模块均未建、也不需要建（粒子内联，其余效果直接在 pvz.c 里画） | 计划已作废 |

**另**：`README.md` 完全未提及代码里已有的 **每日营地 `ST_DAILY`**（`5700-5718`）——属于"做了但没写进文档"，方向相反的一处失真。

---

## D. 上轮 UI 审计发现、尚未修复（延续待办 D1-D8）

来自 `_ui_proof_report.txt` / `_ui_audit_report.txt`（基于程序自导帧的探针色像素实测，非目测）：

### D1. 悬停提示跨层拆分 —— 3 个界面出现"有字无底"（MEDIUM）
`drawHoverTipShapes`（`2052-2086`）画在世界层 `gWorldDC`，`drawHoverTipText`（`2088-2130`）画在 `gFrameDC`。在 `ST_PAUSE` / `ST_WIN` / `ST_LOSE` 三个界面，文字层绘制发生但底板未绘制（或被遮挡），报告 B 节列出 `pvz.c:2101/2108/2114/2117` 共 5 处文字"6 帧全不可见"——实际表现为**提示文字压在画面内容上、没有底板**。
另 `pvz.c:4831`「左键选中后点草坪种下；右键取消选择」同样 3 帧全不可见。

### D2. 真·压字 21 类（其中 2 类必须修）
- `[ST_LOADOUT]` 重叠比 **100%**（2340px²）：`5099`「星星 1 金币 41」压 `5156`「点一下切换候选…」——**两条标题级文字完全叠死**。
- `[ST_REWARD]` 重叠比 **100%**（3480px²）：`5092`「植物抽卡」压 `5177`「通关奖励 · 选一张翻开」——**两个大标题完全叠死**。
- 其余 19 类为 `5763/5764/5765`「225」阳光数字的多重描边互叠（92%/84% 重叠），属**描边写法问题**（描边层与实心层被算成两次独立文字），视觉上可接受，但报告算法把它判为压字，需要连同 #50 的精炼一起确认是否误报。

### D3. 真·越界出血 2 处（LOW）
- `[ST_ACH]` `pvz.c:5154`「已完成 6 / 20」y=636，越出 650 高的画面 **6px**。
- `[ST_LOADOUT]` `pvz.c:5162`「已选候选 4 / 5　实战携带 4 株」越界 **4px**，且与 `5099` 货币行纵向重叠。

### D4-D8. 其余（详见 `_ui_audit_report.txt`）
含 `5763` 阳光数字三重描边、顶部条与内容区间距、右下角 HUD 死代码（= A4，D8 决策项）等。

---

## E. 建议修复顺序

| 优先级 | 项 | 工作量 | 影响面 |
|---|---|---|---|
| P0 | **A1** 僵尸出场池扩到 13 种 | 1 行 + 回归测 | 一局内容量翻 4 倍，README 一整节复活 |
| P0 | **A4** 右下角 HUD 搬到 StretchBlt 之后 | 挪 26 行 + 改 1 处 spriteBlit | 3 货币/2 能量从"不可见"变"可用" |
| P1 | **A2** 接上 `R_FURY` / `R_LOADOUT_PLUS` | 2 行 | 抽卡池去假 |
| P1 | **D2** ST_LOADOUT / ST_REWARD 两组 100% 压字 | 改 4 个 y 坐标 | 两个界面标题可读 |
| P2 | **A3** 关卡 `colFrom/colTo` 接入 `cellLegal` | 2 行 | 6 个关卡玩法差异化 |
| P2 | **D1/D3** 悬停提示跨层 + 2 处越界 | 中 | 3 个界面 |
| P3 | **C** 文档同步（README/PROGRESS/注释） | 低 | 无代码风险 |
| P4 | **B** 视觉升级 P1/P2（逐帧动画、木纹、暗角、拖尾、翻牌） | 高 | 观感 |

---

## F. 已确认「做完了」的部分（供对照，避免重复劳动）

- 40 株植物（8 基础 + 4 新增 + 20 创意 + 8 神级），精灵**全部存在**
- 13 种僵尸枚举 + 精灵 + 倒地帧（`zombieEatFile` 缺独立帧的退化为行走图）
- 76 项遗物定义，74 项已接 `HAS()` 效果钩子
- 6 模式 / 5 武器 / 3 货币 / 2 能量（逻辑层全部实现，仅显示层死在 A4）
- 6 关卡 / 12 天赋 / 20 成就（`achUnlock` 被调用 18 次）/ 抽卡 / 合成
- 三选一：每 2 波触发（`gWave%2==0`，`3243`），必含植物卡保底
- 难度 v3 公式：`2 + idx*1.9 + idx²*0.95 + idx³*0.030`，旗标波 ×1.55，波间隔 23.5/17.2，同波散布窗口 `10-idx*0.27`（下限 3.5）
- 粒子系统：内联实现，30+ 发射点
- 每日营地 `ST_DAILY`（README 未记载）
- 悬停提示系统（存在 D1 跨层缺陷）
