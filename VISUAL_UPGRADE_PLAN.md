# PvZ-C 视觉升级方案

> ⛔ **本计划已作废（2026-09-17 标注），仅作历史存档。**
> 方案里「33 种植物精灵缺失 / 10 种僵尸缺失 / 需新建 particle.c、material.c、animation.c、postprocess.c
> 等 5 个模块」的结论**已全部过期**：
> - `art/raw/` 那 **107 张源图**的数字没错，但**下游资源已全部产出**——`assets/` 现有 **165 张 BMP**：
>   40 株植物 + 13 种僵尸（含 eat / 倒地帧）+ 23 种子弹 + 5 种武器 + 6 个模式图标，全部齐备。
> - 计划中的 5 个新 `.c` 模块**均未建、也不需要建**：粒子系统已内联进 `pvz.c`
>   （`Particle` 结构体 + `addParticle()`，30+ 发射点），其余质感 / 动画效果直接在 `pvz.c` 的绘制函数里实现。
> - 构建仍是单文件：`build.bat` 只编 `pvz.c`，不链接任何 particle / material 模块。
> - 真正落地了哪些视觉升级，见本文末尾「附录：已完成的优化」与 `GAPS.md` 的 B 节。

## 当前状况分析

### 已有资源（107张图片）
- ✅ **子弹特效**：23种子弹已全部生成（pea, ice, kernel, flame等）
- ✅ **基础素材**：阳光、草坪变体、背景部分完成
- ⚠️ **植物精灵**：33种植物中大部分缺失（只有少数AI生成）
- ⚠️ **僵尸精灵**：10种僵尸及其变体（闪白、染蓝、啃食、倒地帧）大部分缺失
- ⚠️ **场景图**：草坪变体、背景图不完整

### 主要视觉问题
1. **缺乏深度感**：程序化图形为主，大量纯色填充，没有光影层次
2. **特效单薄**：
   - 击中反馈弱（只有简单闪白）
   - 无粒子系统（爆炸、烟雾、火花）
   - 无拖尾效果
3. **质感不足**：
   - 卡片是纯色圆角矩形（"塑料感"）
   - 植物/僵尸多为程序化几何图形
   - UI元素扁平、缺乏材质
4. **动画僵硬**：
   - 植物只有简单呼吸（sin波上下浮动）
   - 僵尸行走无帧动画
   - 缺少过渡动画

---

## 分阶段优化方案

### 第一阶段：核心美术资源补全（优先级最高）

#### 1.1 植物精灵生成
**目标**：为33种植物生成高质量、风格统一的精灵图

**执行方案**：
```bash
# 使用 mmx CLI 批量生成
# 风格参考：卡通、手绘感、饱和度高、轮廓清晰

植物类型分组：
- 射手类（豌豆、寒冰、三线、裂荚等）：绿色基调，炮管/发射器特征
- 防御类（坚果、地刺、大蒜）：棕褐色/橙色，厚重质感
- 爆炸类（樱桃、辣椒）：红色/火焰色，能量感
- 特殊类（磁铁、金盏花、催眠菇）：紫/蓝/金色，魔法感
- 英雄植物（火焰英雄、水晶英雄等）：发光、粒子、英雄感

每株植物需要：
- 主体精灵（160×160px，底部中心锚点）
- 透明背景PNG
- 配色与游戏整体色调一致
```

**生成prompt模板**：
```
A cute cartoon [植物名] plant character for tower defense game,
vibrant colors, hand-painted style, clear outlines,
front view, standing pose, bottom-center anchor,
transparent background, 2D game sprite, high detail
```

#### 1.2 僵尸精灵生成
**目标**：为10种僵尸生成完整动画集

**每种僵尸需要**：
- 行走姿态（主精灵）
- 啃食姿态（eating）
- 倒地动画帧×5（dead0-4，旋转倒地）
- 纯白剪影（受击闪白用）
- 冷蓝剪影（减速染色用）

**风格要求**：
- 破烂感、僵尸特征明显
- 与植物风格匹配但有对比（暗色调 vs 亮色调）
- 清晰的轮廓和识别度

#### 1.3 场景与草坪
**目标**：补全5种关卡场景

缺失资源：
- `lawn.png`：标准草坪（5行9列网格）
- `lawn_night.png`：月夜墓园
- `lawn_water.png`：后院泳池
- `lawn_roof.png`：屋顶天台
- `lawn_desert.png`：沙漠遗迹
- `background.png`：通用背景（后院木栅栏）

**规格**：
- 草坪：540×400px（9列×5行，每格60×80）
- 背景：1000×660px

---

### 第二阶段：粒子与特效系统

#### 2.1 粒子系统架构
在代码中添加轻量级粒子系统：

```c
typedef struct {
    float x, y;
    float vx, vy;
    float life, maxLife;
    COLORREF color;
    float size;
    int type;  // 0=圆点 1=火花 2=烟雾 3=星光
} Particle;

#define MAX_PARTICLES 512
static Particle particles[MAX_PARTICLES];
static int particleN = 0;

// 发射器函数
void emitParticles(float x, float y, int count, int type, COLORREF color);
void updateParticles(float dt);
void drawParticles(HDC dc);
```

#### 2.2 关键特效点

**击中特效**：
- 子弹命中：8-12个粒子径向爆开
- 冰冻击中：冰晶粒子+蓝色闪光
- 火焰击中：火花+上升烟雾
- 爆炸伤害：环形冲击波+大量粒子

**植物特效**：
- 向日葵产出阳光：金色粒子上浮
- 樱桃爆炸：红色冲击波+火焰粒子
- 辣椒喷火：持续火焰粒子流
- 寒冰菇：冰晶扩散+霜冻光环

**僵尸死亡特效**：
- 倒地时：尘土粒子
- 爆炸死亡：肢体碎片（简化为几何形状）
- 冻结粉碎：冰块碎片

#### 2.3 拖尾与轨迹
```c
// 为高速子弹添加运动模糊/拖尾
typedef struct {
    float x, y;
    float alpha;
} TrailPoint;

// 每个子弹记录最近N帧的位置，渲染时绘制渐变透明的轨迹
```

---

### 第三阶段：材质与光影

#### 3.1 UI材质升级

**顶部木质条**（当前纯色填充）：
- 添加木纹纹理（横向条纹）
- 边缘高光+阴影（3D感）
- 卡槽凹槽效果

**卡片质感**：
```c
// 当前代码2695行：纯色填充
fillRound(dc, x, y, x + w, y + h, 6, 
          ready ? RGB(238, 224, 178) : RGB(196, 186, 162));

// 改进：渐变+纹理
// 1. 底色打底
// 2. 垂直渐变（顶部亮、底部暗）
// 3. 细微噪点纹理（模拟纸质感）
// 4. 边缘高光（顶部/左侧亮边）
```

**阳光面板**：
- 当前是纯色+几何太阳
- 改进：木框材质 + 布纹内衬 + 3D太阳（发光+阴影）

#### 3.2 植物/僵尸光影

**接地阴影**（已有但可增强）：
```c
// 当前2954行：简单椭圆阴影
static void drawContactShadow(HDC dc, float cx, float y, float w, float h)

// 增强：
// 1. 根据光源方向偏移
// 2. 多层半透明叠加（软阴影）
// 3. 根据物体高度调整模糊度
```

**环境光与边缘光**：
- 植物顶部加亮边（模拟天光）
- 僵尸底部加暗部（增强体积感）
- 夜间模式：整体降低亮度+蓝色调

#### 3.3 后处理效果

**屏幕空间效果**：
```c
// 1. Vignette（暗角）：四周轻微变暗，聚焦中心
void applyVignette(HDC dc);

// 2. 血量低时红色脉冲（已有flashRed，可增强）
// 3. 僵尸破门时屏幕震动+模糊
// 4. 过关/失败时的淡入淡出
```

---

### 第四阶段：动画与过渡

#### 4.1 植物动画增强

**当前**：仅sin波浮动（2786行）
```c
float br = sin(phase * 2.2f);
sy = 1.0f + br * 0.022f;
```

**改进**：
1. **呼吸动画**：x/y缩放不同步（椭圆形变）
2. **待机动画**：
   - 向日葵：花盘缓慢旋转
   - 射手：炮管前后微动
   - 坚果：轻微摇晃
3. **攻击动画**：
   - 射击时：后坐力（瞬间缩小再弹回）
   - 发射瞬间：白色闪光
4. **受伤动画**：
   - 被啃时：红色闪烁+抖动
   - 血量低：枯萎效果（颜色变暗+下垂）

#### 4.2 僵尸动画

**行走动画**：
- 当前只有sin波摇摆
- 改进：逐帧动画或多态插值（头-身-腿不同频率）

**啃食动画**：
- 头部前后运动
- 咀嚼节奏感

**倒地动画**（已有帧序列）：
- 增强：粒子+颜色过渡（变灰+淡出）

#### 4.3 UI动画

**按钮反馈**：
- hover：缩放1.05x + 高光扫过
- click：缩放0.95x + 下沉效果

**卡片抽取**：
- 翻牌动画：3D旋转效果（当前是瞬间切换）
- 光芒粒子+音效提示点

**波次提示**：
- 第10波/最后一波：屏幕震动+红旗特效（已有，可增强）

---

### 第五阶段：性能优化与polish

#### 5.1 渲染优化
```c
// 1. 脏矩形更新：只重绘变化区域
// 2. 对象池：粒子/子弹预分配，避免频繁malloc
// 3. 精灵缓存：相同精灵共享HBITMAP
// 4. LOD：远距离/边缘物体降低细节
```

#### 5.2 细节polish

**屏幕边界**：
- 草坪边缘：软阴影+景深模糊
- 左侧栅栏：3D木纹+钉子高光

**波次进度条**（已有）：
- 当前较简陋，可改进：
  - 金属质感边框
  - 绿色渐变填充（带高光）
  - 旗帜icon改为3D图标

**HUD元素对齐与间距**：
- 检查所有putText调用，确保像素级对齐
- 统一圆角半径、阴影偏移

---

## 实施优先级与工作量估算

### P0（必须完成，直接影响可玩性）
1. **植物精灵生成**（33张）：2-3天
   - 用mmx批量生成
   - 人工筛选最佳结果
   - Photoshop调整透明度/尺寸
   
2. **僵尸精灵生成**（10×6=60张）：3-4天
   - 每种僵尸需要6个变体
   - 保持风格一致性

3. **草坪场景图**（5张）：1天
   - 可用AI生成无缝贴图
   - 确保格子对齐

**P0总计**：6-8天

### P1（显著提升体验）
4. **基础粒子系统**：2-3天
   - 实现Particle结构和更新循环
   - 添加10个关键特效点（爆炸、击中、死亡）

5. **UI材质升级**：2天
   - 木质条纹理
   - 卡片渐变+纸质感
   - 阳光面板3D化

6. **接地阴影增强**：0.5天
   - 多层叠加软阴影
   - 根据物体大小动态调整

**P1总计**：4.5-5.5天

### P2（锦上添花）
7. **植物动画增强**：2天
8. **僵尸动画**：1.5天
9. **UI动画与过渡**：1.5天
10. **后处理效果**：1天
11. **细节polish**：1天

**P2总计**：7天

### 总工作量：17.5-20.5天（约3-4周）

---

## 技术细节与代码改造点

### 代码结构改动
```c
// 新增模块
particle.c      // 粒子系统
material.c      // 材质渲染（渐变、纹理、光影）
animation.c     // 动画状态机
postprocess.c   // 后处理效果

// 修改点
pvz.c:
  - drawPlantEntity()      添加动画状态参数
  - drawZombie()           添加帧动画支持
  - drawHUDShapes()        替换纯色为材质渲染
  - render()               添加粒子层、后处理pass
```

### 性能基准
- 当前GDI渲染：~60fps（简单场景）
- 目标：保持50fps以上（添加粒子和材质后）
- 瓶颈预测：AlphaBlend调用次数（每个半透明粒子一次）
- 优化策略：粒子批处理、精灵图集

---

## 美术资源规范

### 文件命名
```
assets/
  plants/
    peashooter.png          # 豌豆射手
    snowpea.png            # 寒冰射手
    ...
  zombies/
    zombie_basic.png        # 普通僵尸-行走
    zombie_basic_eat.png    # 普通僵尸-啃食
    zombie_basic_dead0.png  # 倒地帧0
    ...
  bullets/
    bullet_pea.png         # 豌豆子弹
    ...
  scenes/
    lawn.png              # 标准草坪
    lawn_night.png        # 夜间草坪
    background.png        # 后院背景
  ui/
    ui_cardback.png       # 卡背
    ui_burst.png          # 爆炸光效
    wood_texture.png      # 木纹纹理
```

### 尺寸规范
- 植物精灵：160×160px（SS=2，实际显示80×80）
- 僵尸精灵：160×200px（高一些）
- 子弹：64×64px
- 草坪：540×400px
- UI图标：128×128px

### 色彩规范
- 植物主色：饱和绿（#7ACC50）、亮黄（#FFE870）
- 僵尸主色：灰绿（#A8B89C）、褐色（#8B7355）
- UI木质：深棕（#684A2C）、浅棕（#926642）
- 高光：白色80%透明
- 阴影：黑色40%透明

---

## 风险与备选方案

### 风险1：AI生成质量不稳定
**缓解**：
- 每个植物生成5-10个候选，人工挑选
- 建立风格参考库（seed images）
- 必要时人工微调（Photoshop）

### 风险2：性能下降
**缓解**：
- 分阶段测试，每加一个特效就跑benchmark
- 粒子数量限制（MAX_PARTICLES=512）
- 提供"低配模式"开关（关闭粒子/材质）

### 风险3：美术风格不统一
**缓解**：
- 先做10%的资源，确定风格基调
- 全员review后再批量生成
- 使用相同的prompt模板和参数

---

## 下一步行动

### 立即可做（今天）
1. **生成第一批植物精灵**（5个常用植物试验）：
   ```bash
   mmx image generate --prompt "A cute cartoon sunflower plant..." \
     --aspect-ratio 1:1 --n 5 --out-dir art/test_plants/
   ```

2. **编写粒子系统框架代码**：
   - 创建particle.c
   - 实现基础发射器和更新逻辑
   - 添加一个测试特效（樱桃爆炸）

### 本周目标
- 完成P0第1项：33种植物精灵全部生成并导入
- 完成P1第4项：基础粒子系统可用，至少5个特效点

### 里程碑
- **Week 2**：P0完成，游戏有完整美术资源
- **Week 3**：P1完成，特效和材质到位
- **Week 4**：P2完成，动画和polish收尾

---

## 附录：已完成的优化

✅ 子弹特效资源（23种全部生成）
✅ 接地阴影系统（基础版）
✅ 植物呼吸动画（sin波浮动）
✅ 僵尸减速/受击染色（spriteTint）
✅ 倒地动画帧序列（5帧旋转）
✅ HUD木质背景（纯色，待升级）
✅ 屏幕震动效果（addShake）
✅ 红色闪光（受伤反馈）
