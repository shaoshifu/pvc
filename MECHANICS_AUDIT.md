# 肉鸽植物、成长卡与僵尸机制修复报告

## 修复结果

- 植物能力位：64/64 已接通。
- 僵尸能力位：A 32/32、B 10/10、C 20/20 已接通。
- Plant 字段：全部读写成对。
- `Zombie.armSwing` 现已由程序化僵尸手臂绘制直接消费，静态审计不再存在只写不读字段。
- 修复肉鸽连锁反弹被子弹初始化二次清零的问题；连锁反弹与武器弹跳/回旋镖改用独立状态，避免互相覆盖。
- 所有手工创建的子弹现在先完整清零，杜绝复用实体槽时继承上一颗子弹的旧状态。
- 召唤与分裂不再错误依赖波次排队数量；召唤单位设 72 只在场上限，控制后期实体压力。

## 本轮实现

- 植物：贷款生命翻倍与结算扣阳光、贪吃成长、复制邻近植物叠层、融合增益、感染邻近僵尸、黑客类范围伤害、子弹僵尸间反弹、步进移动、爬墙移动。
- 僵尸：死亡复生、死亡分叉、死亡诅咒、像素即死免疫、步进移动。
- 僵尸死亡事件统一在 `zombieDie()` 入口处理，避免倒地动画重复触发。
- 像素免疫同时接入像素即死和处决路径。
- 贷款字段 `fuseMul` 已参与植物生命计算；`rgDebt` 已用于波次 / 通关结算。
- 诊断场景按键冲突已修复：K 断电、Q 冰锥受击、Z 肉鸽僵尸；新增 R 死亡事件能力场景。

## 验证

- 静态审计：所有能力位均接通。
- `_rg_verify.py`：抽卡、肉鸽僵尸渲染、进程稳定性全部通过。
- `_verify_mechanics.py`：16 个植物代表机制全部通过。
- `_verify_zombies.py`：生成、品质血量倍率、推进、击杀全部通过。
- `_verify_newz.py`：8 只新僵尸、位移机制全部通过。
- `_test_draft_nav.exe`：18/18 通过，成长卡、免费规则、价格与后期缩放正常。
- `_test_mechanics_quality.exe`：8/8 通过，严格验证连锁跳转、满队列召唤、复生、死亡分叉与死亡诅咒。
- 最终编译：`pvz.exe` 成功，`-Wall -Wextra` 无警告。

## 注意

`_verify_newz.py` 保留画面与位移综合检查；召唤和死亡事件不再依赖它的宽松判据，现由 `_test_mechanics_quality.exe` 对运行时状态做独立、确定性的自动断言。

---

# 规则变更：每一关开局只能选择一个植物

## 为什么要改

原实现里开局携带量是可以被放大的：`relicsRecalc()` 里有一条
`if (HAS(R_LOADOUT_PLUS)) { gCandidateSlots += 1; gCardSlots += 1; }`，
基础值又各是 2 —— 也就是说开局至少能带 2 株。要改成**硬锁 1 株**。

难点不在把数字改成 1，而在于"开局选 1 株"这件事有 **5 个入口**都可能在事后把它改大：
遗物加成、存档旧值、局内奖励解锁、三选一解锁、以及脏存档。只改默认值会漏。

## 唯一真源

`pvz.c` 顶部（`gCardSlots` 定义处）新增：

```c
#define LOADOUT_SLOTS_MAX 1
static int gCardSlots      = LOADOUT_SLOTS_MAX;   /* 战斗中的出战卡位 */
static int gCandidateSlots = LOADOUT_SLOTS_MAX;   /* 开战前的候选池上限 */
static int loadoutCap(void)                       /* 取小 + 夹到 [1, MAX] */
```

所有入口一律走 `loadoutCap()`，不再各写各的：

| 入口 | 文件位置 | 行为 |
|---|---|---|
| `relicsRecalc()` | 取消 `R_LOADOUT_PLUS` 的加卡位分支，两个槽位恒等于 `LOADOUT_SLOTS_MAX` | 遗物无法突破上限 |
| `loadoutBuild()` | 两层循环都用 `n < cap` | 兜底补位也只补 1 株 |
| `loadoutNormalize()` | `if (n > cap)` 判超限 | 旧脏档收敛为"只留索引最小的一株" |
| `loadoutToggle()` | 改成**单选 / 顶替**语义（不再是多选取消） | 大厅点击恒为 1 株 |
| `loadoutEquipOnly(pid)` | 新增 | 局内解锁植物时"顶替"而非"追加" |
| `applyRelic()` / `rewardChoose()` | 改调 `loadoutEquipOnly()` | 奖励解锁不会变成同屏 2 张卡 |

`loadoutToggle()` 必须换成单选取代语义：旧的多选逻辑（点已选中的则取消最后一个）
在 cap=1 时会退化成一页**点不动** —— 已选中的取消不掉、候选池又已满加不进来。

## `R_LOADOUT_PLUS` 为什么不删

`gRelic[]` 按**枚举下标**落盘，删掉或挪动枚举会让旧存档的遗物整体错位
（玩家凭空多出/丢掉遗物）。所以保留枚举位置，只把效果与文案改成
**「随机赠卡」**：获得时白送一张随机肉鸽植物卡（免费，走 `gRgCard` 战斗内卡槽，
**不占**开局那唯一 1 个卡位）。

⚠️ 触发点必须挂在 `applyRelic()` 的"本次首次获得"分支
（`if (isNew && id == R_LOADOUT_PLUS) rgGrantStarterCard();`）。
**不能**放在 `relicsRecalc()`（每帧/每次拿遗物重算，会反复触发），
**也不能**放在 `resetGame()` —— `gRelic[]` 在 `resetGame()` 开头就被 `memset` 清零，
遗物是局内获取、不持久化，放那里 `HAS()` 必然为假，赠卡永远不发生。

## 有意保留的边界

以下**不算**"开局选择"，不占那唯一 1 个卡位，属局内成长，本次不改：

- `gBonusPlant[6]`：三选一拿到神级植物解锁的额外卡槽；
- `gRgCard[8]`：波次奖励的肉鸽植物卡；
- 第 0 波开波前的那次三选一（`gRgDraftWave = -1`）。

## 验证证据链

### ① 运行时断言（无窗口回归测试）

`_test_loadout1.c` → `_test_loadout1.exe`，**25/25 ALL PASS**。覆盖：
默认 cap 与 `loadoutCap()` 均为 1、遗物置位后 cap 仍为 1、
`loadoutBuild` 兜底恰好 1 株、`loadoutToggle` 的单选/顶替/点自身不清空/反复切换不超 1、
未拥有植物点不动、脏档（`loadout[2]/[11]/[77]` + 神级脏值）收敛为只留索引 2、
`loadoutEquipOnly` 顶替与越界保护、`rgGrantStarterCard` 给 1 张且重复拾取不再给、
`resetGame()` 后 `gLoadoutN + gBonusN + gRgN == 1`，
以及**真实 UI 路径**：`ST_LOADOUT` 点卡片 → `ST_LEVELS` 点关卡 → `gState == ST_PLAY` 且卡槽恰好 1 张。

回归：`_test_draft_nav.exe` 24/24 ALL PASS（三选一导航未受影响）。

### ② 截图像素级证据（真实 UI 路径）

`_mk_shot.py` 生成自驱动版 `_shot.c` / `_shot.exe`，`_run_shot.py` 抓图。
输出在 `_frames_loadout1/`：

| 文件 | 内容 |
|---|---|
| `p11_full.png` + `stat11.txt` | 点击前编组页：土豆雷有绿框，`loadout 3 0 0 0` |
| `p10_full.png` + `stat10.txt` | 点击向日葵后：绿框**顶替**到向日葵，`loadout 0 0 0 0`，仍只有 1 个选中 |
| `p01_top.png` + `stat01.txt` | 开局战场：卡槽**只有 1 张植物卡 + 铲子**，`state=1 N=1 bonus=0 rg=0` |

编组页文案：「开局出战植物 **1 / 1 株**　点击卡片切换」。

### ③ 编译

`pvz.exe` 用 `gcc -O2 -mwindows -static-libgcc … -Wall -Wextra` 编译，**零警告**。

## 排查记录：为什么早期的截图脚本一直抓不到图

`_verify_loadout1_shot.py`（已删除）用 `PostMessageW(hwnd, WM_KEYDOWN, VK_F1, 0)`
外部注入热键，`PostMessage` 返回 1、窗口也可见，但 `_frames_loadout1/` 始终为空。
当时怀疑是环境变量没传进子进程、或按键被前置分支吞掉 —— **两个猜测都不对**。

真正的根因在 `render()` 里：

```c
if (gpuDrawFrame(cw, ch, dx, dy, dw, dh)) return;   /* GPU/D3D 路径 */
...
dumpMaybe(gFrameDC);   /* ← 抓帧代码原本挂在这里，GPU 模式下永远执行不到 */
```

窗口标题是「· GPU」，即默认走 Direct3D 呈现。抓帧钩子挂在 `render()` 末尾，
而末尾在 GPU 路径下**根本不会到达** —— 不是按键没送到，是抓帧代码不在执行路径上。

修法（只改诊断版，不动 `pvz.c`）：

1. 钩子前移到 `gpuDrawFrame()` 的提前返回点**之前**；
2. 抓帧源从 `gFrameDC` 改为 **`gWorldDC`** —— GPU 路径读的是 `gWorldBits`
   （`VIEW_W*SS` 的 2x 超采样画布），`gFrameDC` 只在 GDI 回退分支里才被填充，
   拿它截会得到一张空图；
3. 导出目录写成编译期字面量，彻底排除"环境变量没进子进程"这一类干扰；
4. 用进程内按帧号驱动的 `shotAuto()` 重放真实点击，不再依赖外部按键。

**结论：外部 PostMessage 热键在本项目上不可靠（GPU 路径会吞掉抓帧），
后续要做视觉验证请直接用 `_mk_shot.py` + `_run_shot.py` 这套自驱动方案。**

## 存档安全

所有测试都通过 `PVZ_SAVE_FILE` 环境变量把存档隔离到 `_test_loadout1_save.dat`。
测试前后的玩家存档 `pvz_save.dat` md5 恒为 `943352cd4b88e450882f42d39dde8a0e`，
与备份 `_bak_save_before_tests.dat` 一致。

## 改动文件清单

- `pvz.c`（唯一被修改的源文件）
- `_test_loadout1.c`（新增，25 项断言）
- `_mk_shot.py` / `_run_shot.py` / `_conv_shot.py`（新增，自驱动截图链路）
- `pvz.c.bak-pre-1plant`（改前备份）

---

# 追加：殿堂档（RGQ_HALL）+ 下架 4 株 —— 收尾与验证补全

上一段结束时 `pvz.c` 已改完并编译成功，但那只是「代码写完」，
**验证链路整条是断的**。本节记录补全的过程与查出的问题。

## 一、代码侧最终形态（代码本身在上一段就完成了）

### 新增最高档「殿堂」RGQ_HALL
- `RGQ_COUNT` 6 → 7，`RG_PLANT_N` 100 → 105（新增索引 100~104）
- 伤害 **100×** = 传奇 20× 的 **5 倍**；
  **频率与范围刻意停在传奇的 1.60 / 3.00 不动** ——
  三项一起放大，实际强度会从 5 倍膨胀到 25 倍以上
- 出现权重 2（单波命中 ≈0.8%）；售价 500
- ⚠️ `cost` 字段从 `unsigned char` **拓宽到 `unsigned short`**：
  原来填 500 会被**静默截断成 244**

五尊：

| 索引 | 名称 | 基座 | 机制要点 |
|---|---|---|---|
| 100 | 终末裁决 | 玉米炮 | 炮弹即处决判定，落点轨道轰炸，破甲 + 击退，全员增伤光环 |
| 101 | 永恒霜狱 | 冰瓜 | 半径减速 + 蛛网粘滞，冰冻转资源，全队减伤护盾 |
| 102 | 周天星斗 | 杨桃 | 五向花瓣弹幕，随节拍递增，吸血续航，定期召唤星仆 |
| 103 | 曦光裁决 | 激光豆 | 贯穿整行无视护甲，标记增伤，连击叠攻速 |
| 104 | 天罗慑魔 | 猫尾草 | 全屏追踪弹，濒死增伤，增伤 / 护盾 / 阳光三重光环 |

能力位刻意避开 `FUSEPLANT / STEPMOVE / ROOFWALK / BLINK / LOAN /
GAMBLERISK / SELFDESTRUCT` —— 殿堂是「站定输出的核心」，不是整活道具。

### 下架 4 株（软移除）
像素农夫 / 木质守护 / 蜗牛菇 / 烛芯芽。

**为什么不删枚举位**：`gSave.plantOwned[]` / `gSave.loadout[]` 都是按 `PT_*`
下标落盘的，从中间挪走一株，老存档里排在它后面的解锁状态会整体错一位
（玩家会看到「没解锁的变亮、已解锁的变灰」）。
和 `R_LOADOUT_PLUS` 那次是同一类约束：**枚举位一旦落盘就是存档格式**。

做法是 `plantRetired(pid)` 过滤 6 个发牌入口：
抽卡池 / 奖励池 / 神级兜底池 / 编组界面 / 初见赠送 / 收集度统计。
收集度分母改成 `plantPoolTotal()`（运行时统计而非写死常量），
否则「收藏家」成就永远解不开。

## 二、本段补全的验证链路（原先三处全是坏的）

| 问题 | 现象 | 修法 |
|---|---|---|
| `_count_tiers.py` | 直接崩：`KeyError: 'RGQ_HALL'` | 补新增档位 |
| `_ct.exe` | 是旧版（编于改动前），报 `RG_PLANT_N=100 RGQ_COUNT=6` | 重编；顺带修掉 MinGW 控制台 `%ls` **静默输出空串**（改 `WideCharToMultiByte` + UTF-8） |
| 无测试覆盖 | 新增档位与下架逻辑零断言 | 新增 `_test_hall_tier.c`（见下） |

### `_test_hall_tier.c`：40 项断言，ALL PASS，零告警

覆盖范围：

- **下架**：4 株标记数精确、`plantPoolTotal`/`loCount`/`loId` 一一映射无重复、
  越界槽位返回 -1、新档不白送下架株、老档迁移不抹历史解锁、
  神级兜底池在「只剩下架株」时返回 -1、
  三盲盒 ×60 轮与抽卡 ×120 轮都不吐下架株、
  已拥有也点不进编组、大厅点它无效、末页无空洞；
- **殿堂**：档位 / 售价 500 不被截断 / 5 张基座互不相同 /
  105 株名字全局唯一且不与永久植物重名 / 能力不含禁用项 /
  殿堂色比其余六档都亮 / 每档至少 1 株 / 权重全正且殿堂最稀有；
- **编译期断言**（`SA_LEN`）：`RGQ_NAME / COL / DMG / RATE / RANGE /
  TRAITN / W / COST` 这 8 个数组的长度必须都等于 `RGQ_COUNT`，
  `gSprRgPlant` 长度必须等于 `RG_PLANT_N`，
  `plantOwned` / `loadout` 维度必须 ≥ `PT_COUNT`。
  **这一族数组没法 assert、编译器也不报越界，只能靠手动对齐** ——
  所以让编译期就拦住。

## 三、三个此前没人发现的真问题

### P0-A 真存档被测试程序清成新档

现象：`pvz_save.dat` 从「87 株解锁 / 18 星 / 3 关」变成「21 株 / 0 星 / 3 金币」＝一个全新档。
时间戳两次：**13:57** 与 **15:54**（后者是本轮跑回归造成的）。

根因：**`_test_draft_nav.c` 与 `_test_weapons.c` 都没设 `PVZ_SAVE_FILE`**。

- `_test_draft_nav.c` 会走 `rgOpenDraft` / 三选一选择路径，那些分支里有 `saveFlush()`；
- `_test_weapons.c` 用 `CreateProcessW(L"pvz.exe", …, lpEnvironment = NULL)` 拉子进程，
  **环境被继承** ⇒ 子进程直接写玩家真档。

已修：两者都在**进入游戏逻辑之前**设 `PVZ_SAVE_FILE`。
验证：重编后跑完全部测试，真档 md5 恒定不变。

> `_rg_verify.py` 之前踩过同一个坑并已修，这次是另两处漏网。
> **规则固化：任何会进游戏逻辑的测试程序，第一件事就是设 `PVZ_SAVE_FILE`。**

存档已从 `_bak_save_before_tests.dat` 恢复，并用 `_check_save.c` 校验：

```
ver=6  stars=18  coins=38  xp/lv=71/18
owned=83 株（不含已下架的 4 株）
loadout= 1 个入选 -> #0 向日葵     ← 单选收敛正确
```

`_check_save.c` 特意只读**副本**：`saveLoad()` 之后紧跟 `loadoutNormalize()`
（真实启动路径就是这样），而收敛会 `saveFlush()` 落盘 ——
直接指真档会在校验的同时把它改掉，这类「校验顺手写盘」正是今天真档被清的同一类原因。

### P0-B `_rg_verify.py` 一直零帧（两处独立原因）

- **原因一：抓帧钩子挂在 `render()` 结尾** —— GPU 路径在中途
  `if (gpuDrawFrame(...)) return;` 就返回了，结尾永远到不了。
  这与 `_mk_shot.py` 那次是**同一个坑**，只是 `_mk_framedump.py` 没跟着修。
  已改：钩子前移到 GPU 提前返回点**之前**，抓帧源从 `gFrameDC` 改成 **`gWorldDC`**
  （GPU 上传的就是这块 `VIEW_W*SS` 的 2x 画布，草坪 / HUD / 三选一面板全画在它上面；
  `gFrameDC` 只在 GDI 回退分支才被填充，拿它截是空图）。
- **原因二：外部 PostMessage 热键不可靠** —— 钩子修好后仍然零帧。
  用「开局即抓帧」的探针分离验证：**无按键也能出图** ⇒ 钩子没问题，是键没送到。
  已改：`_mk_framedump.py` 新增 `dumpAuto()`（`PVZ_AUTOSEQ=1`），
  **进程内按帧号直接调游戏函数**；`_rg_verify.py` 改成单次等待。

结果：**3/3 全部通过**。

### P1 抓帧版热键表里有两条死分支

`VK_F6`（→选关）与 `VK_F10`（编组，重复绑定）各被绑定两次，
**先绑定的胜出**，后绑的那条永远执行不到。已删除死分支并加注释。
同类情况还有一处保留：`V` 键同时被「切第 6~10 关」和「验证神级植物」占用，
前者胜出 —— 已在代码里标注清楚，避免以后照着死分支去排查。

## 四、殿堂五尊美术资源

`assets/rgplant_100..104.bmp` **此前不存在**，缺图会**静默退回基座贴图**
（不报错，五株只是长得像豌豆/西瓜/杨桃/激光豆/猫尾草）。

已生成并入库：尺寸 125~200 px，不透明率 27~49%，全部通过回读校验。
`_gen_units.py` 同步扩容（`RG_PLANTS_N=105`、第 7 档 flair、5 条视觉钩子）。

实机验证：新增 `D` 键 / 自动序列 tag 23 摆出五尊 + 巨人 + 普通僵尸，
抓帧确认五张全部正确加载渲染（`art/panels_hall_ingame.png`）。

详细踩坑记录见 `PIPELINE.md` §6，其中最重要的一条：
**「最高档」提示词里写发光/光辉，模型必在脚下画光环或发光底盘**，
三代提示词都压不住，最终用 `art/_strip_pinkhalo.py` 做确定性后处理。

## 五、本段最终验证状态

| 检查 | 结果 |
|---|---|
| `pvz.c` 编译（`-Wall -Wextra`） | 零警告 |
| `_test_hall_tier.exe` | **40/40 ALL PASS** |
| `_test_loadout1.exe` 回归 | 25/25 ALL PASS |
| `_test_draft_nav.exe` 回归 | ALL PASS（且不再污染真档） |
| `_rg_verify.py` | **3/3 全部通过** |
| `_ct.exe` / `_count_tiers.py` | 与源码一致（105 株 / 7 档） |
| `_check_save.exe` | PASS（恢复后的真档） |
| 真档隔离 | 跑完全部测试 md5 不变 |
| 殿堂贴图实机渲染 | 5/5 正确加载 |

## 六、本段改动文件

**新增**：`_test_hall_tier.c`、`art/_strip_pinkhalo.py`、`_check_save.c`
**修改**：`_mk_framedump.py`（钩子位置 + `gWorldDC` + `dumpAuto` + `D` 键 + 死分支清理）、
`_rg_verify.py`（进程内驱动）、`_count_tiers.py`、`_ct.c`、
`_test_draft_nav.c` / `_test_weapons.c`（补存档隔离）、
`art/_gen_units.py`（殿堂档）、`PIPELINE.md`（§6 批次记录）
**未改**：`pvz.c`（本段一行未动）
**新增资源**：`assets/rgplant_100..104.bmp`、`art/gen2/u_p100..104.jpg`、
证据图 `art/panels_hall_onlawn.png` / `art/panels_hall_ingame.png`

---

# 追加：三选一出牌门槛（去掉普通档）

需求：把三选一里的**普通档肉鸽植物**去掉，让其他档位概率整体上升。

## 实现方式：过滤发牌口，不动 rgPlants[]

```c
#define RG_DRAFT_MIN_TIER  RGQ_RARE      /* pvz.c 顶部，改这一个常量即可 */
static int rgDraftEligible(int idx)      /* 紧跟 rgOwned() */
{
    if (idx < 0 || idx >= RG_PLANT_N) return 0;
    return (int)rgPlants[idx].tier >= (int)RG_DRAFT_MIN_TIER;
}
```

接在 `rgOpenDraft()` 的候选池构造上：

```c
for (i = 0; i < RG_PLANT_N; i++)
    if (!rgOwned(i) && rgDraftEligible(i)) cand[n++] = i;
```

**为什么不是从 `rgPlants[]` 里删掉那 14 条**：删掉会让后面所有索引整体前移，
而贴图是按下标命名的（`rgplant_NN.bmp`，见 `gSprRgPlant` 的加载循环），
索引一错，剩下 91 株会全部贴上错误的图，且**不报任何错**。
肉鸽植物不落盘，所以这里没有存档格式约束，只有**贴图索引**约束。

## 故意没有一起改的地方：遗物赠卡

`rgGrantStarterCard()`（只被遗物 `R_LOADOUT_PLUS` 触发，一局最多一张）
**保留**了普通档。原因：它不参与三选一的抽卡、不稀释其他档位概率，
而保留它能让那 14 株不至于变成彻底无法获取的死内容。
要连它一起禁，改一行即可（函数内已注明）。

## 改动前后（解析值，实测已交叉校验）

合格池 105 → **91 株**；总权重 2004 → **1584**。

| 档位 | 单卡位（改前） | 单卡位（改后） | 一次三选一 2 张至少见（改后） |
|---|---|---|---|
| 普通 | 20.96% | **0%（不上牌）** | 0% |
| 稀有 | 31.14% | **39.39%** | 63.7% |
| 史诗 | 25.95% | **32.83%** | 54.8% |
| 传说 | 13.47% | **17.05%** | 31.5% |
| 神级 | 6.49% | **8.21%** | 15.9% |
| 传奇 | 1.50% | **1.89%** | 3.7% |
| 殿堂 | 0.50% | **0.63%** | 1.25% |

整局 20 次至少见一次：传说 99.0% → **99.8%**、神级 88.6% → **94.0%**、
传奇 38.2% → **45.4%**、殿堂 14.1% → **18.2%**。

## 验证

`_test_draft_odds.c` 新增第三段校验：

```
== 出牌门槛校验（20000 次三选一）==
  合格池 91 株 / 全表 105 株；实际出现过 91 株
  低于门槛的卡出现次数 = 0   PASS
```

回归：`_test_draft_nav.exe`（三选一导航）、`_test_hall_tier.exe`（40 项）、
`_test_loadout1.exe`（25 项）全部 ALL PASS；`_rg_verify.py` 3/3 通过；
`pvz.c` 零告警；真档 md5 未变（`943352cd…`）。

改动文件：`pvz.c`（+ 备份 `pvz.c.bak-pre-tierfloor`）、`_test_draft_odds.c`。

---

# 追加：档位概率重定（神级 10% / 传奇 8% / 殿堂 5%）

用户指定目标：单卡位整档概率 **神级 10%、传奇 8%、殿堂 5%**，
其余三档吸收剩余 77%，并保持「越稀有越低」的降序不变。

## 一、先做了两件必须做的事

### 1. 拆出独立权重数组，避免"改植物把僵尸带跑"

`RGQ_W` 原来被两处共用：`rgOpenDraft()`（植物三选一）和 `rgPickZombie()`（肉鸽僵尸）。
直接改它会让肉鸽僵尸的档位构成一起跳变（神级僵尸 3.5%→10%、传奇 1.25%→8%），
属于"改一个地方、动两套平衡"。

所以拆成两个数组，并立下约定写进注释：

| 数组 | 服务对象 |
|---|---|
| `RGQ_W` = `{30,26,20,15,10,6,2}` | **肉鸽僵尸**（`rgPickZombie`），本次**未改动** |
| `RGQ_DRAFT_W` = `{30,30,23,14,16,33,21}` | **三选一植物**（`rgOpenDraft`） |

测试里有一条断言专门守这件事：`肉鸽僵尸权重 RGQ_W 未被改动 PASS`。

### 2. 目标锚在"档位概率"，所以权重**故意不是单调递减**

档位概率 = 该档株数 × 权重 / 总权重，而各档株数差别很大
（神级 13 株、传奇/殿堂各 5 株）。
想让只有 5 株的传奇拿到 8%、比 13 株的神级还难抽，
**传奇的单株权重就必须高于神级**。于是新数组不是降序的：

```
稀有30 / 史诗23 / 传说14 / 神级16 / 传奇33(最高) / 殿堂21
```

这不是写错，是"目标锚在档位概率而不是权重"的必然结果，已写进代码注释。

## 二、★ 关键发现：总权重凑成 2 的幂，RNG 偏差直接归零

抽取用 `rndi(0, tot)`，实现是 `rand() % tot`，而 MSVCRT 的 `RAND_MAX` 只有 32767。

- 第一版按目标算出的总权重是 **2602**：`32768 / 2602 = 12.59`，留下余数段
  （低段 544 个取值多拿一次，偏重 8.3%），实测把**殿堂从设计值 5.00% 压到 4.73%**；
- 改成 **2048 = 2^11**（`32768 / 2048 = 16` 整除，余数段长度为 0）后，
  实测值与设计值完全对齐：殿堂 **5.112%** vs 设计 5.127%。

**结论（写进注释）：改这套权重时必须让
`24wR+26wE+18wL+13wM+5wU+5wH` 恰好等于 2 的幂**（1024/2048/4096 都行，
但别越过 `RAND_MAX/2`，否则后半段候选永远抽不到）。

## 三、最终数值

合格池 91 株，总权重 **2048**。

| 档位 | 株数 | 权重 | 单卡位概率 | 一次三选一 2 张至少见 | 整局 20 次至少见 |
|---|---|---|---|---|---|
| 普通 | 0（门槛外） | 30 | 0% | 0% | 0% |
| 稀有 | 24 | 30 | **35.16%** | 58.2% | 100% |
| 史诗 | 26 | 23 | **29.20%** | 50.3% | 100% |
| 传说 | 18 | 14 | **12.30%** | 23.3% | 98.4% |
| 神级 | 13 | 16 | **10.16%**（目标 10） | 19.4% | 96.9% |
| 传奇 | 5 | 33 | **8.06%**（目标 8） | 15.4% | 92.9% |
| 殿堂 | 5 | 21 | **5.13%**（目标 5） | 10.0% | **81.0%** |

合计 100.00%，硬指标偏差 ≤0.16pp，严格降序。

### ⚠️ 必须提醒的副作用：整局出现率暴涨

「5%」是**单个卡位**的概率。一局有 20 次三选一、共 30 个植物卡位
（前 10 波 2 张、后 10 波 1 张），所以累计出现率是
`1-(1-0.05)^30 ≈ 79%`：

| 档位 | 改前整局 | 改后整局 |
|---|---|---|
| 神级 | 88.6% | **96.9%** |
| 传奇 | 38.2% | **92.9%** |
| 殿堂 | 14.1% | **81.0%** |

**殿堂从「四局一见」变成「几乎每局都有」。**
若希望的是「整局 5% 才见一次殿堂」，那单卡位应设为约 **0.17%**，
需要另给一组数值。

## 四、验证

```
== 目标校验（解析口径，合格池 2048 总权重）==
   实际 10.16% 神级   目标 10.0%  偏差 +0.16pp
   实际  8.06% 传奇   目标  8.0%  偏差 +0.06pp
   实际  5.13% 殿堂   目标  5.0%  偏差 +0.13pp
  合计 100.00%  PASS
  稀有>史诗>传说>神级>传奇>殿堂 严格降序：PASS
  肉鸽僵尸权重 RGQ_W 未被改动：PASS
== 出牌门槛校验（20000 次三选一）==
  低于门槛的卡出现次数 = 0   PASS
```

回归全绿：`_test_draft_nav`、`_test_hall_tier`(40 项)、`_test_loadout1`(25 项)
ALL PASS；`_rg_verify.py` 3/3；`pvz.c` 零告警。

## 五、附带发现：真档在 18:09 被写成空档（非测试程序所为）

排查过程：把 8 个测试程序逐个跑一遍、每次比对真档 md5
（`_test_draft_odds / draft_nav / hall_tier / loadout1 / seedbar / mower /
mechanics_quality / weapons`），**全部不泄漏**；
会真正运行游戏二进制的 `_rg_verify.py` 也确认不泄漏（它先拷贝真档再设
`PVZ_SAVE_FILE`）。所以不是自动化链路干的，已从备份恢复
（md5 回到 `943352cd…`）。若 18:09 前后手动启动过 `pvz.exe` 并出现进度丢失，
那是一个独立的加载/重置问题，需要单独查。

---

# 追加：始祖级（RGQ_PRIMORDIAL）三尊 + 主题切换

用户定义：新增 3 株最强植物（地刺 / 坚果 / 豌豆炮 各一，档位名「始祖」），
**出现概率 1%、强度是其他植物的 1000 倍、多种能力超级叠加、售价 10000 阳光**，
且出现时**必定改写背景与音乐**，此后不再变化，直到下一个始祖级出现。

## 一、档位表扩到 8 档

新增 `RGQ_PRIMORDIAL`（索引 7，排在殿堂之上），同步扩了 **9 个数组**：
`RGQ_NAME / COL / DMG / RATE / RANGE / TRAITN / W / DRAFT_W / COST`。
`RGZ_MINWAVE / RGZ_COST` 是按 `[RGQ_COUNT]` 声明的，多出来的档位自动补 0，无需改。

三尊（索引 105~107）：

| 索引 | 名称 | 基座 | 售价 | 能力 | 伤害 |
|---|---|---|---|---|---|
| 105 | 始源棘原 | 地刺 PT_SPIKEWEED | 10000 | 64 位全开 | 1000× |
| 106 | 太初坚壁 | 坚果 PT_WALLNUT | 10000 | 64 位全开 | 1000× |
| 107 | 混沌齐射 | 豌豆炮 PT_PEASHOOTER | 10000 | 64 位全开 | 1000× |

- **"多种能力超级叠加"直接给 `RG_ALL_TRAITS`（64 位全开）**，
  不再逐个 OR —— 以后再加新人能力位也不会漏掉这三尊。
- `RGQ_TRAITN` 填 64，卡片上"能力 64 条"是字面事实。
- **1000× 是真的 1000×**：`rgDmgMul()` 对植物是 `直接返回 RGQ_DMG[tier]`，
  没有压缩（那层 `1+(x-2)*0.42` 的压缩只作用于**僵尸血量** `rgZombieHpMul`）。
  实测一株始祖上场即秒杀巨人。

### ★ 售价 10000 的两个坑（都踩过）

1. **`rgPlantCost()` 的 `CLAMP(cost, 50, 500)` 会把 10000 静默夹成 500。**
   已把上界抬到 10000。这和当年殿堂 500 被 `unsigned char` 截断是同一类问题：
   数值能编译、能运行、不报错，只是悄悄变成另一个数。
2. **这个游戏里三选一本来是"每波免费"的**（`gDraftFree`），
   所以只写售价等于这个数字没有任何意义。
   已把**始祖设为唯一不免费的档位**：`applyRelic` 里对始祖传 `freebie=0`，
   走 `rgGrant` 的收费通道 —— 阳光不足时 `gBuyFailed` 置位、不扣款也不发卡，
   不会出现"标价 10000 实际白拿"或"扣了钱没卡"。

## 二、概率：总权重又回到 2 的幂

目标：始祖 1%、神级 10%、传奇 8%、殿堂 5%，其余三档吸收剩余 76%，降序不变。

- 档位概率 = **株数 × 权重 / 总权重**，而各档株数差得极远
  （史诗 26 株、神级 13 株、传奇/殿堂各 5 株、**始祖只有 3 株**）。
  要让 3 株的始祖只占 1%、5 株的传奇占 8%，它们的单株权重必须**高于**株数多的档位 ——
  所以权重数组**故意不是单调递减的**：`114 / 86 / 70 / 63 / 131 / 81 / 27`。
- 总权重从 2048 抬到 **8192 = 2^13**（`32768 / 8192 = 4` 整除）。
  这一步不是凑数：`rndi(0,tot)` 是 `rand() % tot`，MSVCRT 的 RAND_MAX 只有 32767，
  总权重不能整除 32768 就会留下余数段、偏低段取值偏重。
  实测值因此与设计值完全对齐（见下表）。

下表全部是 `_test_draft_odds.exe` 的**实测值**（单卡位 20 万次采样 / 整局 2 万局），
不是手算的设计值：

| 档位 | 株数 | 权重 | 单卡位 | 一次三选一 2 张 | 整局 20 次至少见 |
|---|---|---|---|---|---|
| 稀有 | 24 | 114 | 33.65% | 56.27% | 100% |
| 史诗 | 26 | 86 | 27.26% | 47.35% | 100% |
| 传说 | 18 | 70 | 15.26% | 28.29% | 99.56% |
| 神级 | 13 | 63 | **9.93%**（目标 10） | 18.94% | 96.85% |
| 传奇 | 5 | 131 | **7.99%**（目标 8） | 15.48% | 92.36% |
| 殿堂 | 5 | 81 | **4.91%**（目标 5） | 9.61% | 77.89% |
| **始祖** | 3 | 27 | **0.99%**（目标 1） | 1.97% | **25.79%** |

设计值（解析口径）与实测的偏差 ≤0.06pp、合计 100.00%、严格降序 —— 全部记录在
`_test_draft_odds.c` 的"目标校验"段，每次运行都会重新核对。

> ⚠️「1%」是**单个卡位**的概率。一局 30 个植物卡位，累计
> `1-(1-0.01)^30 ≈ 26%` —— 即**平均每 4 局能见到一次始祖**（实测 25.79%）。

## 三、主题切换（背景 + 音乐）

| 环节 | 做法 |
|---|---|
| 触发 | `rgOpenDraft()` 填完 `gDraft[]` 后调 `draftScanPrimordial()`：三个选项里只要有一张始祖卡就切主题。抽成独立函数是为了**可测**（测试可以直接摆 `gDraft[]`，不必靠 1% 运气） |
| 背景 | 新增草坪变体 `LV_VARIANT_PRIMORDIAL = 10` + 贴图 `lawn_primordial.bmp`。走的是既有的 `gLawnVariant` 通路，缺图会自动退回程序化草坪 → **先上代码后补美术是安全的** |
| 音乐 | `bgmPlayFile(L"bgm/primordial.mp3")`。文件缺失时它自己 return，不会崩 |
| 持久 | 局内全局 `gPrimordialTheme`：`bgmForLevel()` 开头就检查它（换关不换音乐）；关卡启动改走 `levelLawnVariant(lv)`（换关不换背景）。**刻意不回退** —— 用户要求"除非出现下一个始祖级，不然不会再变化" |
| 再现 | 每次始祖出现都重新执行一遍（重放音乐 + 全屏闪一次），所以"下一次出现才再变"是肉眼可见的 |

**音乐是程序化合成的**（`art/_gen_bgm.py`）：本机 mmx CLI 没有音乐生成能力，
五关那批 mp3 也没留下脚本。这支曲子用 numpy 合成 7 层
（27.5/55Hz 次低音 + 失谐 drone + 小调 pad + 高频微光 + FM 钟 + 48Hz 心跳脉冲 + 低通噪声涨落），
再过 Schroeder 混响，两端淡入淡出，76 秒，最后用 ffmpeg 编码成 mp3。
**必须用 mp3 不用 wav**：本项目实测 MCI 播 WAV 时 `play ... repeat` 返回错误 259（打开成功但不出声），
mp3 走 mpegvideo 解码器，repeat 正常。

## 四、验证

新增 `_test_primordial.c`（**16 项，ALL PASS**）：
三尊档位/基座/售价/64 位能力/1000× 伤害、主题触发与不触发、
换关不洗掉主题、之后的普通三选一保持开启、再次出现会重新执行、
价格门（9999 拿不到 / 10000 扣光拿到 / 免费规则对始祖无效 / 其他档位仍免费）。

回归：
| 检查 | 结果 |
|---|---|
| `pvz.c` 编译 `-Wall -Wextra` | 零警告 |
| `_test_primordial.exe` | **16/16 ALL PASS** |
| `_test_hall_tier.exe` | **48/48 ALL PASS** |
| `_test_draft_odds.exe` | 目标校验全 PASS（始祖 0.99% / 偏差 -0.01pp） |
| `_test_draft_nav.exe` / `_test_loadout1.exe` | ALL PASS |
| `_rg_verify.py` | 3/3 |
| 真档 `pvz_save.dat` | md5 恒为 `943352cd…` 未被触碰 |
| 实机抓帧 | `plants=3`、`killed=2`、`bgm open=1 mode=playing` |

### 顺手修掉的过时断言

`_test_hall_tier.c` 里有 8 条上一批写的断言，加入始祖后**静默失效**了：
`RG_PLANT_N == 105`、`RGQ_COUNT == 7`、"殿堂是最亮的色"、
"最后五条都是殿堂"（范围写的是 `RG_PLANT_N-5`，加一批就会检查到别的档位）、
"所有档位权重为正"（始祖没有僵尸，权重按设计就是 0）等。
已逐条按**新事实**修正（改口径、改成显式索引区间 100~104），
其中"颜色最亮"改成两条真正成立的不变式（始祖最亮 + 殿堂比下面六档都亮）——
没有为了让它变绿而放宽标准。

## 五、已知残留

三尊贴图脚下仍有**光环/亮盘**（白圈 + 粉雾），与殿堂那批是同一类问题：
模型把"最高档角色"翻译成"脚下要有发光台座"，三代提示词压不住。
已用 `art/_strip_pinkhalo.py` 清掉粉雾（命中 2.3% / 9.3% / 0.2%），
亮环因为是**与主体连通**的（主干域占 99.5%+）无法用连通域剔除，保留。
详见 `PIPELINE.md` §6/§7。

## 六、改动与新增文件

**改**：`pvz.c`（备份 `pvz.c.bak-pre-primordial`）、`_mk_framedump.py`（'T' 键 + `PVZ_AUTOSEQ=2` 序列）、
`_test_hall_tier.c`、`_test_draft_odds.c`、`art/_gen_units.py`、`_gen_art.py`、`_install_art.py`
**新增**：`_test_primordial.c`、`art/_gen_bgm.py`
**新增资源**：`assets/rgplant_105..107.bmp`、`assets/lawn_primordial.bmp`、
`assets/bgm/primordial.mp3`、证据图 `art/panels_primordial_onlawn.png` / `_ingame.png`

---

# 追加：修复「最后两只僵尸打不死」（第 24/24 波卡死）

用户报障：末波剩两只僵尸怎么打都不死，关卡结算不了（附了截图）。

## 一、复现（先复现，再修）

写了 `_test_zombie_stall.c`：把每种肉鸽僵尸单独放进受控场景，
**用它自己的基座**（铁桶/巨人的血量远高于普通僵尸，一律用普通僵尸会低估），
跑真实的 `terrainUpdate()`（含兜底防卡死）+ 真实的 `rgZombieTraitTick()`，
再叠加一路固定火力，记录阵亡耗时。

修复前的实测：

| 场景 | 2 万僵尸中最坏 | 说明 |
|---|---|---|
| 弱火力 200 DPS | **尸王 75.3 秒** | 火力完全打不动，只能等兜底 |
| 零火力（只靠兜底） | **88.1 秒** | 兜底被自愈反超，前 10 多秒压不下去 |

也就是说：这两只是**真的可以永远打不死**（在弱火力下），用户的报障成立。

## 二、根因：自愈速率 > 兜底伤害速率

单个僵尸最多能同时挂 **4 个自愈源**，而且全部按"脉冲"结算：

| 来源 | 每脉冲回血 | 触发条件 |
|---|---|---|
| 保命组 `FAKEHP/CHISHIELD/DODGE30/HEALBACK/REFLECT/ENTANGLE/FUSEZOMBIE` | 10% basehp | hp < 70% maxhp |
| `RZ3_SHIELD` 护盾 | 12% basehp | hp < maxhp |
| `RZ3_REGEN` 自愈 | 9% basehp | hp < 85% maxhp |
| `RZ3_TAUNT` 嘲讽 | 5% basehp | hp < maxhp |
| **合计** | **36% basehp / 脉冲** | |
| `RZ3_AURA` 光环（给同行 260px 内**其他**僵尸） | 4% × 同伴 | 同伴 hp < maxhp |

脉冲周期 = `4.6 - 0.42 * tier` 秒 —— 传奇档 **2.5 秒**：

```
36% / 2.5s ≈ 14.4% 最大血 / 秒
```

而兜底防卡死的起始速率只有 **7%/秒**（且要 75 秒才开始）。
**自愈跑赢兜底 → 两者在 75~95 秒之间打平 → 关卡永远结算不了。**

> 代码里其实已经踩过一次同类坑（`terrainUpdate` 的注释、以及 RZ2_ALL 被削掉保命能力
> 都是为此），但那次只处理了"单只僵尸挂满 64 位"的极端组合，
> **没有给"多个自愈源叠加"设总量上限** —— 尸王/铁壁这类天生带 2-3 个自愈源的仍在翻车。

## 三、修复

1. **给自愈设"每脉冲总量上限"**：`ZOMBIE_HEAL_CAP_PER_TICK = 5% maxhp`。
   四个自愈源**共享同一份额度**（新增 `zombieHealBudget()`，脉冲开始时建额度、用完为止）。
   光环给同伴的回血算在**被治疗者**头上并同样封顶 ——
   否则两只光环僵尸同排就能互相刷血，又回到原点。
   5% 换算成速率是 2%/s（传奇档），比兜底的 9%/s 低 4.5 倍，两者不会再打平；
   同时 5% 仍是有感的续航，不是把自愈砍没了。
2. **兜底提前并加强**：起始 75 → **60 秒**，速率 7% → **9%/秒**，强杀 105 → **90 秒**。
   60 秒仍远长于正常一波（20~35 秒），不影响常规战斗。
3. 顺手修正 `period` 的注释（写的是"传奇 2.1s"，公式实际给 2.5s）。

## 四、修复后

`_test_zombie_stall.exe`（**5 条断言 ALL PASS**，覆盖 70 种僵尸 × 3 种场景）：

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 弱火力 200 DPS，单只 | 尸王 **75.3 s** | 尸王 **68.4 s** |
| 强火力 800 DPS，同型两只同排 | — | 塔防合成僵尸 **33.8 s** |
| 零火力（只靠兜底） | **88.1 s** | **70.9 s** |
| 活满 120 秒仍不死的类型数 | 0（但会拖到 88 s） | **0** |

「同型两只」是专门为用户的场景加的：两只同排可以互相吃光环回血，
是最容易互相保命的组合，实测最坏 33.8 秒清掉。

## 五、★ 探针本身的陷阱（值得单独记一笔）

第一版探针跑出来 **70 种僵尸最坏耗时 0.2 秒、全绿** —— 看着完美，其实毫无意义。

原因：僵尸血量 = `zHp[t] * gZombieHpMul * 波次系数`，而 **`gZombieHpMul` 是全局变量、
只在 `resetGame()` 里被置 1**。无窗口测试不走 `resetGame()`，
于是所有僵尸血量都是 **0**，一生成就"死"，测出来的是空气。

**教训：无窗口测试摆僵尸/植物之前，必须先走一遍 `resetGame()`**，
否则拿到的是"看起来通过、其实什么都没测"的绿灯。
这类"全 0 输入导致的假通过"比失败的测试危险得多。

## 六、附带发现（未改，待决策）

`RZ3_REBORN`（复生：死亡后满血复活一次）在代码里被注释成
"由 zombieDie() 统一处理"，但 **zombieDie() 里没有任何 RZ3_REBORN 分支** ——
也就是说 07 概率坩埚僵尸 / 66 不死僵尸 的"复生"是**空转的**，
描述与实现不符（它们实际比说明弱）。

**本次没有实现它**：用户要的是"别再打不死"，此时给僵尸加一条复活是反方向的动作。
要做的话正确写法是复用 `z->rgRevive`（只触发一次），
并且兜底的强杀路径用 `byMower=1` 已经会绕过所有复活 —— 需要单独确认再动。

## 七、真档 md5 基线变更说明（不是丢失）

本轮发现 `pvz_save.dat` 的 md5 从 `943352cd…` 变成 `4f31d8e9…`。
逐字节比对：**唯一差异是 `loadout[18]` 由 1 变 0** ——
用户启动游戏时，"每关只能选 1 株"的收敛逻辑把它从两株收敛成一株并落盘。
进度（87 株解锁 / 18 星 / 38 金币 / 4 关）完好，**不是清档**。
以后判断"存档有没有被动过"不能只看 md5，要看**关键字段**。

## 八、改动文件

`pvz.c`（备份 `pvz.c.bak-pre-stallfix`）、新增 `_test_zombie_stall.c`。
`pvz.exe` 已重编（零告警）；回归 `_test_primordial` / `_test_hall_tier` /
`_test_draft_odds` / `_test_draft_nav` / `_test_loadout1` 全 PASS；真档未被动（md5 不变）。

---

# 追加：吞噬系植物审计（静态 + 运行时两道闸门）

用户要求："检查各类吞噬系植物，是否功能真实有效"。

## 一、静态闸门：能力位接通了吗？

剥掉 `#define` 行与全部数据表后统计实现区引用：

| 能力位 | 实现区引用 | 挂载植物 |
|---|---|---|
| `RG_T_DEVOURBUL` 吞噬来弹转阳光 | 1 处 | 03 黑洞磁力菇、48 吞噬黑洞葵 |
| `RG_T_GROWEAT` 吃僵尸后成长 | 1 处 | 05 贪吃蛇大嘴花、32 镜像大嘴、82 食人花圃、83 永生藤、98 创世树 |
| `RG_T_FUSEPLANT` 吞噬相邻植物融合能力 | 1 处 | 27 塔防合成人、98 创世树 |

三个位**都有实现引用**，静态闸门通过。
（另有 12 个"未被消费"的位全是 `RG_T_*_SAFE` 别名，逻辑里用的是真名，属正常。）

**但"接通了"≠"实现了"** —— 必须走第二道闸门。

## 二、运行时闸门：实测三个机制全是假的

新增 `_test_devour_plants.c`，在受控场景里逐个量化。
**修复前**的实测结果：

| 机制 | 描述说的 | 实测发生的 |
|---|---|---|
| `DEVOURBUL` | 吞噬范围内所有来弹 | **停在面前的豌豆毫发无损**（仍在=1）；产阳光量在有弹/无弹下**完全相同**（6 / 6）→ "转化为阳光"不存在 |
| `GROWEAT` | 吞掉僵尸后体型与伤害成长 | **空场（没吃任何东西）40 个脉冲就 ×10.29**（1.06⁴⁰）→ "吞掉后"这个前提从未被判断；而且**无上限**、每脉冲回满血 → 无敌植株 |
| `FUSEPLANT` | 吞噬相邻植物，融合其全部能力 | **邻居存活=1**（根本没被吞）；自身 maxhp **5 个脉冲 ×3.05**（1.25⁵）→ 无上限叠乘；`fuseMul` 是**死写入**（只在出场时被读过一次）；"融合全部能力"零实现 |

三类问题，性质不同：
1. **DEVOURBUL**：被并进了一条 `SHARESUN|EATSUN|BLACKHOLE|AURABUFF|CHI|DEVOURBUL`
   的"产阳光"大分支，于是它只蹭到了阳光，**吞噬动作整段缺失**。
2. **GROWEAT / FUSEPLANT**：都写成"每脉冲无条件叠乘 + 回满血"，**既没有触发前提，也没有上限**。
   这是本文件反复出现过的同一类坑（僵尸侧 `RZ_MUTATE` 有 2.5 倍封顶、`rgStack` 有 5 层封顶，
   唯独植物侧这两个源漏了）。

## 三、修复

`Plant` 结构新增三个字段：`rgBaseHp`（出场血量基准）、`rgEatPend`（确实吞掉的计数）、`rgFused`（只吞一次）。

| 机制 | 修复内容 |
|---|---|
| `GROWEAT` | ① **补上触发前提**：新增 `rgNotifyDevourNear()`，在 `zombieDie()` 里"真正死亡"那一刻给半径 150px 内带 GROWEAT 的植物记账（小推车碾死的不算"被吞噬"）；② **封顶 2.5 倍出场血量**（与僵尸侧 `RZ_MUTATE` 同一档）；③ 一个脉冲只兑现一次成长（限速） |
| `FUSEPLANT` | ① **真吞**：邻居**消失**（`removePlantAt`）；② **只吞一次**（`rgFused`，不再每脉冲叠乘）；③ 封顶 2.5 倍 |
| `DEVOURBUL` | **补上真吞噬**：吃掉飞向本株的敌方子弹（`vx < 0`），每吃一发额外产一份阳光。<br>**原有的自产阳光保持不变**（不削减任何既有收益）；我方射出的子弹不会被误吃 |

另外把 4 张卡片的文案改成与实现一致（原文写的"融合其全部能力""伤害永久成长"都无法兑现）：
03「吃掉飞来的子弹转化为阳光，同时自产少量阳光」/ 05「吞掉附近的僵尸后体型变大（上限 2.5 倍）」/
27「吞噬相邻的那株植物，一次性强化自身（上限 2.5 倍）」/ 98「吞掉身边的一株植物，立即强化自身（上限 2.5 倍）」。

## 四、修复后验证（`_test_devour_plants.exe`，14 条断言 ALL PASS）

| 判据 | 修复前 | 修复后 |
|---|---|---|
| 空场 40 脉冲是否成长 | ×10.29（假成长） | **×1.00**（不吃不长） |
| 喂 30 只僵尸后 | — | **×2.50**（封顶生效，无上限时会是 ×5.7 且继续涨） |
| FUSEPLANT 邻居是否被吞 | 存活=1 | **存活=0**（真吞掉），血量只 ×1.25 一次 |
| 来弹是否被吞 | 仍在=1 | **仍在=0**（真吃掉） |
| 吞噬是否带来阳光 | 有弹 6 / 无弹 6（无关） | 有弹 **7** / 无弹 6（多一份） |
| 我方出弹是否被误吃 | — | **不被吃**（vx>0 跳过） |

## 五、⚠️ 本次未能替换 pvz.exe

`gcc -o pvz.exe` 报 `Permission denied` —— **用户当时正开着游戏**，exe 被占用。
新包已出到 **`pvz_new.exe`**（同源、零告警）。关掉游戏后覆盖即可，
或直接运行 `pvz_new.exe`。

## 六、附带：真档仍在正常推进（不是被清）

`pvz_save.dat` md5 又变了（`4f31d8e9…` → `1b2003a5…`），但关键字段是**增长**的：
星 18→41、金币 38→67、解锁植物 87→89、解锁关卡 4→6。← 用户在正常游玩。
**再次印证：判断存档状态要看关键字段，不能只看 md5。**

## 七、改动文件

`pvz.c`（备份 `pvz.c.bak-pre-devourfix`）、新增 `_test_devour_plants.c`、产出 `pvz_new.exe`。

---

# 追加：全局机制审计（用户："还有哪些没生效的，全局仔细检查每种机制"）

方法：三道静态闸门 + 一道运行时闸门。**报告数字全部来自脚本，不靠读代码猜。**

## 一、审计工具本身踩的两个坑（先说，否则结论会反向）

`_audit_traits.py` 第一版结论是"88 个能力位零消费"——**完全错误**。两个原因：

1. **表尾判据写错**：用"任意 `};`"当表尾，函数内的局部数组会把几百行实现一起剥掉。
   → 改成只认**行首无缩进**的 `};`。
2. **单行表没识别**：`static const int RGZ_MINWAVE[RGQ_COUNT] = { 2, 5, 8, 12, 15, 18 };`
   是**一行写完**的，扫描器找不到独立的 `};`，于是**吞掉了后面 3816 行代码**。
   → 声明行自带 `};` 时当场判定为单行表。

修好后：**数据表 26 张 / 2374 行；逻辑区 10558 行；126 个能力位全部有实现区引用（零遗漏）**。

> 教训：审计脚本本身也要有"数量级自检"。88 个位全空 = 不可能，先怀疑工具。

## 二、静态闸门三项结果

| 检查 | 结果 |
|---|---|
| 能力位是否被实现区引用 | 126 / 126 全部有引用（**通过**） |
| 字段读写成对（Plant / Zombie） | **无"只写不读"、无"只读不写"**（通过） |
| 空壳实现（`if (位) { 只有注释 }`） | **命中 1 个：`RZ3_REBORN`** |

## 三、★ 系统性根因：能力位"共用一段通用实现"

静态引用检查全绿，但**逐位对照"卡片描述 vs 实现动作"**后发现了真正的问题：
大量能力位并不是各写各的，而是**几十个位共用同一段兜底代码**，
所以"位是通的，机制是空的"：

| 组 | 共用的位 | 实际都只做了 |
|---|---|---|
| 控制组 | `TIMEFREEZE` 时停 / `GRAVITY` 重力眩晕 / `LULLABY` 摇篮曲沉睡 / `WEBSLOW` 蛛网 / `CONFUSE` 混乱 / `MEMESPREAD` 模因 / `ICERESOURCE` | **同一段"范围减速 + 定身"** |
| 防御组 | `MIRROR` 镜像 4 倍血 / `FAKEHP` 假血 / `DIGITRAIN` 数字雨 / `DODGE` 相位闪避 / `MAZEWALL` 迷宫墙 | **同一段"自身回 5% 血"** |
| 爆发组 | `GAMBLEDMG` 概率坩埚 / `GAMBLERISK` 赌徒 / `PIXELCRUSH` 像素化 / `ASSASSIN` 暗杀 | **同一段"概率 ×10 的脉冲"** |
| 抽卡组 | `DROPCARD` 抽卡樱桃 / `DRAWCARD` 卡牌射手 | **同一段"给阳光"** |
| 其余 | 上面没被排除的位 | 落到**通用兜底**：一次 `1.6+0.35×档位` 倍的脉冲伤害 |

具体到卡片，就是这些"承诺了但没做"：
- 镜像墙坚果「血量 4 倍」→ 从来没乘 4
- 相位花「打不到本体」/ 概率墙「30% 完全免疫」→ 只有回血，**根本没有闪避/免疫**
- 迷宫坚果「生成迷宫路障」→ 只有回血，**没有路障**
- 抽卡樱桃「掉落一张植物卡」/ 卡牌射手「抽一张牌」→ 只会给阳光
- 噪音向日葵「混乱走位」→ 只有减速，僵尸路线完全不变
- 重力玉米「坠落眩晕」→ 只有定身，没有冲击
- 蜘蛛丝缠绕「留下蛛网陷阱」→ 只有减速，**地上什么都没留**
- 模因传播葵「传播阳光」→ **一点阳光都不产**
- 数字雨「生成屏障」→ 只有回血，`p->shield` 从来没被设置
- 赌徒豌豆「翻倍或归零」→ 共用概率坩埚的 ×10，既没归零也没翻倍
- 概率坩埚「50% 打出 10 倍」→ 实际 `20+4×档位`%（神级 36%）
- 暗杀豌豆「隐身三次，一击必杀」→ 实际是固定 ×18 伤害
- 音乐节拍豌豆「随节拍增伤最高 3 倍」→ **借 `p->shield` 当节拍层数，而 shield 的消费点
  是 `CLAMP(shield/2200, 0.25, 1.0)`，3.0 连下限 0.25 的起点都够不到 → 等于空转**

## 四、本轮修复（每条都有运行时断言）

| # | 机制 | 修复内容 |
|---|---|---|
| 1 | `RZ3_REBORN` 复生 | 真正实现"死亡后满血复活一次"（与 REVIVE3 共用 `rgRevive` 闸门）；小推车/兜底强杀仍绕过复活，不会把"打不死"重新引回来 |
| 2 | `RG_T_RHYTHM` 节拍 | 改用独立字段 `rgBeat`（原借 shield 被 clamp 地板吃掉）+ **真的乘进伤害**（上限 3 倍） |
| 3 | `RG_T_GAMBLERISK` 赌徒 | 真正的 **50% 翻倍 / 50% 归零**，与概率坩埚分离 |
| 4 | `RG_T_WEBSLOW` 蛛网 | 真的在僵尸脚下铺 `TF_WEB` 地形（走地块系统，踩上去会粘） |
| 5 | `RG_T_CONFUSE` 混乱 | 真的把僵尸**往回推**，走位真的乱 |
| 6 | `RG_T_GRAVITY` 重力 | 眩晕附带真实击退 |
| 7 | `RG_T_MIRROR` 镜像 | **出场时真的 ×4 血量**（实测 73600 = 基准 18400 × 4） |
| 8 | `RG_T_DIGITRAIN` 数字雨 | 真的授予 `p->shield` 屏障（阻挡啃食 + 提供伤害减免） |
| 9 | `RG_T_MEMESPREAD` 模因 | 补上"传播阳光"（原来一次都不产） |
| 10 | `GAMBLEDMG` 数值 | 按卡片改成 50%（原来 20+4×档位） |
| 11 | 文案对齐 8 张 | 抽卡樱桃 / 卡牌射手 / 天气葵 / 相位花 / 概率墙 / 迷宫坚果 / 镜像墙坚果 / 暗杀豌豆 —— 文案改成与实现一致，不再骗人 |

验证：`_test_mech_fix.exe` **13 条断言 ALL PASS**（含"僵尸必须带肉鸽 traits"这种探针自检）。
复核 `_audit_noop.py`：**空壳能力位 = 0**。

## 五、仍未实现（需要新机制，不在本轮硬做）

| 项 | 为什么没做 | 建议 |
|---|---|---|
| `DODGE` 真闪避 / `FAKEHP` 假血 | 植物侧**没有统一的受伤入口**（扣血分散在啃食、巨人砸击等 6+ 处分支），加闪避需要在每处插钩子 | 先抽一个 `plantTakeDamage()` 收口，再实现 30% 免疫 |
| `MAZEWALL` 迷宫路障 | 地块系统只有"地形效果"（减速/掉血），没有"阻挡型"地块 | 需要新增可阻挡地块类型 |
| `DROPCARD` / `DRAWCARD` 真掉卡 | 游戏里没有"卡牌掉落物"这一实体 | 需要新的拾取物系统 |
| `LULLABY` 沉睡 / `TIMEFREEZE` 真冻结 / `ICERESOURCE` | 目前与"减速+定身"共用实现，"沉睡/冻结"要区分状态语义 | 需要新的僵尸状态位（如 `asleep`） |
| `WEATHER` 真天气 | 天气层本身就是纯视觉（只画花瓣/雪花） | 已把文案改准；要做真天气需新增效果 |

## 六、改动文件

`pvz.c`（备份 `pvz.c.bak-pre-globalfix`）、新增 `_test_mech_fix.c`、
新增审计脚本 `_audit_traits.py` / `_audit_noop.py` / `_audit_fields.py` / `_audit_pairs.py`。
`pvz.exe` 已重编（零告警）；回归 8 个测试全 PASS；真档字段正常增长（41 星 / 89 株）。

---

# 追加：第二轮复查（"再复查一次"）

第二轮把审计面从"肉鸽能力位"扩到**遗物 / 天赋 / 规则 / 模式 / 地形 / 武器**系统，
并复核上一轮修好的东西有没有退化。

## 一、复核结果：上一轮的修复全部保住

| 闸门 | 结果 |
|---|---|
| 空壳实现（`if(位){只有注释}`） | **0 个**（上轮修掉 `RZ3_REBORN` 后保持为 0） |
| 能力位是否被实现区引用 | 126 / 126，零消费 **0 个** |
| Plant / Zombie 字段读写成对 | 无"只写不读"、无"只读不写" |
| 运行时 8 个测试 | **全部 exit=0** |

天赋 `TAL_*`（7）、规则 `RULE_*`（6）、地形 `TF_*`（5）、武器 `WPN_*`（7）、
模式 `MODE_*`（7）—— 逻辑区引用均为 0 遗漏。

## 二、★ 本轮新发现（P0，比上轮所有问题都严重）：**遗物表与枚举错位**

`relicDefs[R_COUNT]` 是**按位置对齐**的数组（行里不写枚举名），
而它的行顺序与 `enum { R_NONE = 0, ... }` 的顺序**不一致** ——
枚举把 `R_BANKER` / `R_NECROMANCER` 放在索引 9 / 10，
奖励表却把「银行家」「摄魂者」放在 45 / 46，于是中间整段平移。

**用运行时取真值（不靠解析，避免误判）：**

| 枚举位 | 该位实际生效的效果 | 表里显示的卡名 | 应该是 |
|---|---|---|---|
| 9 `R_BANKER` | 解锁金气 | 「磨刀石」 | 银行家 |
| 11 `R_PEA_DMG` | 豌豆伤害 ×1.12 | 「疾风」 | 磨刀石 |
| 25 `R_THORNS` | 反伤 22 | 「霜冻」 | 荆棘 |
| 29 `R_IRON_WALL` | 坚果生命 +120 | 「烈性火药」 | 铁壁 |
| 45 `R_MINIMAL` | 费用 -35% / 冷却 +60% | 「银行家」 | 极简主义 |
| 46 `R_NO_COOLDOWN` | 冷却 -45% / 生命 -30% | 「摄魂者」 | 无冷却 |

对齐的只有首尾两段（0~8、74~76）。**中间约 38 个遗物：玩家看到的卡名和描述，
与真正生效的遗物不是同一个。**

判定依据：拿 `relicsRecalc()` 里 `HAS(R_X)` 的实际效果（如 `gDmgMul *= 1.12f`）
去比对表里的描述（如「豌豆伤害 +12%」）—— 语义能对上，但**索引对不上**，
所以这不是"效果没实现"，是**贴错了名字**。这类 bug 静态检查完全看不出来
（表和枚举各自都是自洽的）。

**为什么这轮才查出来**：上一轮我只查了 `#define` 出来的能力位，
没查**枚举 + 按位置对齐的数组**这一组合。

## 三、修复方案（需要你点头，我不盲改）

我不打算在复查这一轮手动重排 77 行 —— 一旦某行对错顺序，会引入新的错位，
而静态检查查不出来。正确做法是**从结构上消灭它**：

1. 先把每一行按"效果 ↔ 描述"配对（依据 `relicsRecalc()` 里的 `HAS()` 效果）；
2. 改写为**指定初始化**：`[R_PEA_DMG] = { L"磨刀石", ... }` ——
   顺序不再承载语义，**以后插行也不会错位**；
3. 逐行加 `/* R_X */` 注释，让人工一眼能核对；
4. 加一条运行时自检（`_test_relic_align.c`）：对每个锚点断言
   `relicDefs[X].name` 与预期卡名一致，并把 77 项全量打印出来人工过一遍。

**说一句我就按这个做**（约 38 行重排 + 逐条核对）。

---

# 追加：遗物表错位（P0）—— 已修复

用户批准后按方案执行。

## 一、修法：改成指定初始化，从结构上消灭错位

`relicDefs[R_COUNT]` 原来是**按位置对齐**的（行里不写枚举名），行顺序一旦和 enum 不同就静默错位。
现在 77 行**全部改成指定初始化**：

```c
[R_PEA_DMG] = { L"磨刀石", L"豌豆伤害 +12%", 0, 0 },
```

顺序不再承载语义 —— **以后在中间插行、调顺序都不会再错位**。表头也加了说明，写清这个历史 bug。

配对依据：拿 `relicsRecalc()` 里 `HAS(R_X)` 的实际效果（如 `gDmgMul *= 1.12f`、`gThorns = 22`、
`gBanker = 1`）去比对表里每一行的描述（「豌豆伤害 +12%」「反伤 22」「解锁金气系统」），
得到 **76 行 ↔ 76 个枚举槽位的 1:1 映射**（校验过：无重复、无遗漏）。

## 二、修复后（运行时取真值，不靠解析）

| 枚举位 | 效果 | 现在显示的卡名 |
|---|---|---|
| 9 `R_BANKER` | 解锁金气 | **银行家** ✔ |
| 11 `R_PEA_DMG` | 豌豆伤害 ×1.12 | **磨刀石** ✔ |
| 25 `R_THORNS` | 反伤 22 | **荆棘** ✔ |
| 26 `R_MEDIC` | 每 12 秒治疗 | **前线医疗** ✔ |
| 29 `R_IRON_WALL` | 坚果生命 +120 | **铁壁** ✔ |
| 30 `R_SLOW_LONG` | 减速持续 +50% | **霜冻** ✔ |
| 45 `R_MINIMAL` | 费用-35%/冷却+60% | **极简主义** ✔ |
| 46 `R_NO_COOLDOWN` | 冷却-45%/生命-30% | **无冷却** ✔ |

新增 `_test_relic_align.c`：**18 个锚点断言 + 完整性检查（无空项 / 无重名）+ 77 项全量打印**，ALL PASS。

## 三、过程中我自己踩的两个坑（值得记）

1. **第一次改写漏掉了占位行**：表里 `/* R_NONE */ { L"", L"", 0, 0 },` 不以 `{` 开头，
   被我的"以 `{` 开头的行"判据跳过 → 整表偏移 1，编译直接报 `-Woverride-init`。
2. **第一次改写吃掉了行尾逗号**：用 `^(\{.*\})` 捕获时把 `},` 的逗号留在了外面 → `expected '}' before '['`。
   → 正确做法是**只在第一个 `{` 前插入前缀**，其余（逗号、行内注释、缩进）一律不动。

两者都被编译器拦住了 —— 这也说明**编译 + 运行时探针**比人工 review 可靠。

## 四、另：8 个"零引用"遗物其实是误报

第一轮审计报的 `R_VOLLEY / R_COLUMN / R_EMPTY / R_FLOWER_SEA / R_WALLNUT_DOGMA /
R_SYMBIOSIS / R_VANGUARD / R_BACKLINE` 没有 `HAS()` 调用 ——
复核后确认：它们在奖励表里**有对应卡名**（荆棘? 不，是同列共鸣/留白/花海/坚果教条/温床/尖兵/纵深防御/齐射），
只是效果写在别处（`HAS()` 之外的方式）。**不是缺陷**，是审计判据不全导致的误报。
（真正的错位是上面那 ~38 个，已经修好。）

## 五、改动文件

`pvz.c`（备份 `pvz.c.bak-pre-relicalign`）、新增 `_test_relic_align.c`。
`pvz.exe` 已重编（零告警）；9 个回归测试全 PASS；真档 41 星 / 89 株未动。

---

# 追加：3 选 1 遗物「是否真的生效」全量运行时验证（并修复 1 个死代码 bug）

用户要求：全局检查三选一里的遗物是否真的生效，有问题直接修。

## 一、方法：对每一条遗物做"装上前后必须不同"的对照

新增 `_test_relic_effect.c`：覆盖**全部 76 条**可抽到的遗物。每条走三步
① `resetGame()` → 清空 `gRelic` → `relicsRecalc()` → 读基准值
② 只把 `gRelic[id]` 置 1 → 再 `relicsRecalc()` → 读同一个量
③ 前后必须不同（差值就是这条遗物的效果强度）

其中一批改的是"命中/入场时才生效"的量，单独搭场景测：
`齐射`（该行 ≥4 射手 → `rowRateMul`）、`同列共鸣 / 留白 / 花海 / 坚果教条 / 温床 /
尖兵 / 纵深防御`（`fieldDmgBonusAt`）、`寒霜核心`（减速僵尸挨一发看掉血差）、
`隐忍者`（复刻"不结算冷却"的分支）、`以战养战`（击杀后叠层）。
效果写在 `applyRelic()` switch 里的（武器三条 / 模式两条 / 随机赠卡）走**真实发放路径**。

## 二、★ 查出的真 bug：`水中模式` / `夜间模式` 是死代码

```c
static void resetGame(void) {
    ...
    memset(gRelic, 0, sizeof(gRelic));        /* ← 7581：先清空本局遗物 */
    ...
    relicsRecalc();
    ...
    if (HAS(R_WATER_MODE)) { ... }            /* ← 7628：再检查遗物 —— 永远为假！ */
    if (HAS(R_NIGHT_MODE)) { ... }
}
```

遗物**只可能在本局中途**通过三选一拿到，而这两处检查在"新一局开始"时执行、
且执行前刚把 `gRelic` 清空 —— **`HAS()` 恒为假**，两张卡从设计上就不可能生效。

**修复**：改为"拿到卡即解锁并切换"，挪进 `applyRelic()` 的 switch
（与 `R_MUTANT_SEED` 完全一致的写法），并在 `resetGame()` 原处留下注释说明为什么删。

```c
case R_WATER_MODE: gUnlockedMode[MODE_WATER] = 1; gCurMode = MODE_WATER; break;
case R_NIGHT_MODE: gUnlockedMode[MODE_NIGHT] = 1; gCurMode = MODE_NIGHT; break;
```

验证：修复前 `0.0000 → 0.0000`（毫无变化）；修复后 `0.0000 → 1003.0000` / `1004.0000`
（已解锁 + 模式已切到水中/夜间）。

## 三、我自己在写测试时踩的坑（值得记）

`applyRelic()` 的效果被 `int isNew = !gRelic[id]` 守着，而我的 measure() 在"装上前"
那一次调用里已经把 `gRelic[id]` 置了 1 → `isNew=0` → 整段 switch 被跳过 →
**武器架 / 主射升级 / 武器大师 / 模式切换器 / 变异种子 / 随机赠卡 / 水中 / 夜间**
一度全部报"没有效果"。

修正方式是让探针**走真实发放路径**：`on=0` 直接返回 0，`on=1` 先把预置撤掉再 `applyRelic(id)`。
这条经验很重要 —— **"先自己把状态摆好、再调用接口"会让守卫条件失效，
测出来的不是玩家路径**。

另外 `花海` 我一度探错了量：它加的是**植物最大生命**（走 `plantsRefreshHp()`），
不是伤害加成。**探针必须对着实现真正改的那个量。**

## 四、结果

```
覆盖 76 条 / 未检测到效果 0 条
probe: all 76 draftable relics were exercised          ok
effect: every draftable relic changes at least one quantity  ok
ALL PASS (0 failures)
```

10 个回归测试全 PASS；`pvz.exe` 已重编（零告警）；真档 41 星 / 89 株未动。
备份 `pvz.c.bak-pre-modefix`。

---

# 追加：僵尸机制全局审计 + 移除「僵尸远程伤害植物」

用户要求：① 全局检查僵尸机制是否生效；② 去掉僵尸远程伤害植物的机制。

## 一、僵尸机制审计结果（静态两道闸门）

| 检查 | 结果 |
|---|---|
| 僵尸侧能力位（`RZ_*` / `RZ2_*` / `RZ3_*`，共 63 个） | **逻辑区零消费 0 个** |
| 空壳实现（`if(位){只有注释}`） | **0 个** |
| 全部能力位（植物 + 僵尸，126 个） | 零消费 0 个 |

唯一"未被引用"的是 `RZ2_ALL` —— 那是给"终极奇葩"用的组合宏，
之前已刻意弃用（它的注释写明"改成明确的终局攻击组合，不含无限续航"），**属预期**。

## 二、移除僵尸远程伤害植物

### 做法：一个总开关 + 伤害转"压制"

```c
#define ZOMBIE_REMOTE_PLANT_DMG 0        /* 0 = 僵尸只能靠贴身啃食造成伤害 */
#define ZOMBIE_SUPPRESS_TIMER   0.6f     /* 远程能力改为推后植物 timer（秒） */
```

**为什么不直接删代码**：集中一处、改一个数字就能回退，不用翻 7 个地方；
每处伤害点由它统一守着，不会出现"漏改一处"的半吊子状态。

**关键设计：不是简单关掉，而是把"伤害"换成"压制"** ——
关掉伤害后这些能力会变成空壳，玩家什么都感觉不到。
现在它们改成把植物的 `timer` 往后推（攻速与产出变慢），
所以：植物**不掉血**、但你能明显感到"后排被压制、射得慢了"。

### 被改的 7 处（覆盖 22 种僵尸）

| 组 | 能力 | 原行为 → 现行为 |
|---|---|---|
| 光束/弹幕/踢击 | `RZ_LASERCUT` 激光、`RZ_KNIFEBARRAGE` 菜刀弹幕、`RZ2_BOMBBARRAGE` 轰炸、`RZ2_COMBOKICK` 连击踢、`RZ2_BLACKHOLE` 黑洞、`RZ_ASSASSIN` 暗杀 | 隔空扣血 → 推后 timer，保留粒子特效 |
| 中毒/噪音/停产/天气 | `RZ2_POISON`、`RZ_NOISESLOW`、`RZ2_HACKSUN`、`RZ2_WEATHER` | 扣血 → 推后 timer |
| 劫持 | `RZ_HIJACKPLANT`、`RZ2_HACKSUN` | 扣血 → 推后 timer ×1.4 |
| 回溯 | `RZ2_REWINDPLANT` | 削 28% 最大生命 → 只保留"技能延后"（原本就有） |
| 投掷 | `RZ3_THROW` | 隔防线砸掉血 → 保留"东西砸过去"的表现 + 推后 timer |
| 毒云 | `RZ3_POISONCLOUD` | 毒雾烧植物 → 保留地面 `TF_CORRODE` 毒雾 + 推后 timer |
| 磁力 | `RZ3_MAGNET` | 扣血 → 推后 timer（植物是格子制的、没有像素坐标，本来就拽不动） |

### 保留（近战，属于核心玩法）

普通**啃食**、**巨人砸击**、`RZ3_DEVOUR` 速食、`RZ3_FREEZEBITE` 冰噬、大蒜换行。
判定依据：这些都用「僵尸前方 24px 落在植物所在格」，
也就是**僵尸必须已经贴到植物身上**。
关卡规则里的时空裂隙 / 星陨（对植物和僵尸**同样**重创，注释写明"敌我不分"）也不属于僵尸远程攻击，保留。

### 描述同步（14 条）

僵尸卡片原来写着「劫持植物」「切割植物」「秒杀一株植物」「范围摧毁整列」「持续掉血」等，
现在都不成立，已全部改成如实的"压制节奏"表述。复核脚本确认：
**僵尸表里再无"掉血 / 摧毁 / 秒杀 / 切割植物"类承诺。**

## 三、验收测试（`_test_melee_only.exe`，7 条断言 ALL PASS）

| 判据 | 结果 |
|---|---|
| A 僵尸钉在 5 格外跑 30 次能力脉冲 | 植物掉血 **0**（70 种全查） |
| B 端到端：僵尸自己走过去，植物掉血那一刻必须有僵尸贴身 | 远程漏网 **0 种**；正常近战掉血 **60/70 种** |
| C 远程能力没有变成空壳 | **14 种**僵尸会真的推后植物节奏；远程掉血 0 |
| D 开关值 | `ZOMBIE_REMOTE_PLANT_DMG == 0` ✔ |

> ⚠️ 测试 B 我第一版写错了判据：去追"某一只僵尸的位置"，
> 结果尸王（`RZ3_SUMMON`）**召唤的小僵尸**在啃植物、本体还在 124px 外，
> 被误判成"远程"。正确判据是**遍历所有僵尸看有没有贴身**，
> 而且必须用与游戏一致的啃食判定 `(z->x - 24 - LAWN_X) / CELL_W == 植物格`。

## 四、⚠️ 平衡影响（未擅自补偿，等你决定）

22 种僵尸的"远程拆植物"变成了"拖慢节奏"，**僵尸整体威胁明显下降**。
按你的要求这就是目标（提升体验），但如果觉得太轻松，可选补偿方向：

| 方向 | 说明 |
|---|---|
| 僵尸血量 / 移速微调 | 让它们更快贴脸，把压力移到近战 |
| 波次预算或出怪密度 | 数量补偿单体威胁的下降 |
| 啃食伤害 | 贴脸后更疼，强化"必须及时补种"的紧迫感 |
| 什么都不动 | 就是想要更轻松的体验 |

改动文件：`pvz.c`（备份 `pvz.c.bak-pre-remotedmg`）、新增 `_test_melee_only.c`。
`pvz.exe` 已重编（零告警）；**11 个回归测试全 PASS**；真档 41 星 / 89 株未动。

---

# 追加：iOS/IPA 移植 —— Phase 1（资源压缩）完成

用户提出要做能在 iPad 上安装的 IPA（自己签名）。详见 **`IOS_PORT.md`**。

## 为什么先做资源压缩

BMP 是未压缩格式，795 张贴图 259.3 MB，占了整个包的大头。
IPA 体积直接影响签名与安装体验，所以这是移植前性价比最高的一步。

| | 之前 | 之后 |
|---|---|---|
| 贴图 | 259.3 MB（795 BMP） | **47.6 MB**（774 PNG + 21 JPEG） |
| 全包（含 54 MB 音频） | 约 313 MB | 约 **102 MB** |

规则：带透明的精灵 → PNG；整屏不透明的（草坪/菜单背景，21 张）→ JPEG q92。

## ★ 预乘 alpha：这一批最容易做错的地方

这些 BMP 是 32 位 BGRA 且 **alpha 已预乘**（引擎用 `AlphaBlend`，它要求源是预乘的）。
常规的"反预乘 → 存 PNG → 加载时再预乘"**会丢数据**：

```
A=1, RGB=200  →  反预乘 200*255/1 = 51000  →  截断到 255  →  预乘回 1  ≠ 200
```

低 alpha 的描边像素会整片变暗。所以 PNG 里**直接存预乘后的字节**（PNG 不校验 RGB ≤ A），
加载时只做一次 BGRA↔RGBA 字节交换 —— 全程零误差。

## 验证（`_test_asset_load.c` + Python 逐字节比对）

| 路径 | 结果 |
|---|---|
| PNG（带透明精灵，9 张样本） | 与源 BMP **逐字节一致** |
| JPEG（整屏背景，3 张样本） | 平均色差 1.1~2.5（肉眼不可分）；**上下翻转 / R-B 互换的差值大 10~80 倍** → 方向与通道顺序都没错 |
| 实机抓帧 | 电路草坪（JPEG）+ UI + 植物贴图（PNG）全部正确渲染 |
| 全部回归测试 | 全通过（12 个） |

## 过程中修掉的两个自己的错

1. **打包脚本不清理另一种扩展名**：中断留下的 795 个残留 PNG 让加载器优先命中 `.png`，
   于是 JPEG 优化"看起来没生效"（报 0 张 JPEG）。已修成"产出即删另一种"，否则
   `.png` 与 `.jpg` 同时存在会造成歧义。
2. **验证程序把资源目录与输出目录搞混**：`gAssetDir` 指成了输出目录 → 12 个样本全部加载失败。

## 下一步

Phase 2（`plat.h` 可移植层 + iOS 后端）、Phase 3（Xcode 工程 + GitHub Actions 出未签名 IPA），
方案与边界见 `IOS_PORT.md`。

---

# 追加：始祖级强化 + 三选一开牌庆典特效

用户要求：① 提高始祖级整体强度与伤害，做到"与其他植物强度不相匹配、需要过强"；
② 重新设计并增强"抽到殿堂级以上"时的特效表现，要炫酷、有冲击力。

## 一、数值强化（刻意做成"版本之壁"）

| 项 | 之前 | 现在 | 与上一档（殿堂）之比 |
|---|---|---|---|
| `RGQ_DMG` 伤害倍率 | 1000 | **4000** | 殿堂 1000 → **4×** |
| `RGQ_RATE` 攻速倍率 | 1.60 | **3.00** | 殿堂 1.60 → **1.9×** |
| `RGQ_RANGE` 范围倍率 | 3.00 | **8.00** | 殿堂 3.00 → **2.7×** |
| `RGQ_TRAITN` 能力条数 | 64 | 64（不变） | 已是全开 |

综合倍率：伤害 4× × 攻速 1.9× × 范围 2.7× ≈ **相对殿堂再强约 20 倍**；
相对传说档（11×/1.30/1.90）≈ **1400 倍**。

### ★ 顺带堵掉一个隐患：`rgZombieHpMul` 的档位耦合

`RGQ_DMG` 被**两处**消费：`rgDmgMul`（植物伤害）与 `rgZombieHpMul`（僵尸血量）。
僵尸侧公式是 `1 + (RGQ_DMG[tier] - 2) * 0.42`，如果哪天有人加一只始祖僵尸，
血量会变成 **1680 倍**，关卡直接无解。

今天全表**没有**僵尸使用 `RGQ_PRIMORDIAL`（已全表核对），但"以后加一只始祖僵尸"
是个很自然的动作。已在 `rgZombieHpMul` 里钉死上界：
`if (tier >= RGQ_PRIMORDIAL) tier = RGQ_HALL;` —— 越界按殿堂档结算（保守降级，不会静默失控）。

## 二、始祖威压：只有始祖才有的全局被动

单纯堆数字只是"数字大"，玩家不一定能感觉到。所以再加一条**机制层面只有始祖才有**的被动：

> 场上只要有 1 株存活的始祖植物，**全场僵尸持续受到 5.5% 最大生命的真伤，并被持续压制减速**。

- **用百分比而不是固定值**：僵尸血量随波次放大，固定值后期等于没有；百分比不受放大影响。
- **真伤（直接扣 hp）**：绕开护甲减伤，符合"没有东西能挡住它"的定位；
  但**刻意不给任何免死/复活效果** —— 避免又踩到"最后一只打不死"那个坑。
- **双株模式的第二株放在 `grid2`**，统计存活数时两格都数，否则会漏掉一半。
- 战斗中 HUD 顶部显示「始祖威压 ×N」并随时间脉动，让玩家知道僵尸为什么自己掉血。

实测：无始祖时 1 秒掉血 **0**；有 1 株始祖时 1 秒掉 **5.50%**（设计值 5.5%），且减速 0.90。

## 三、开牌庆典演出（殿堂级以上）

触发：本次三个选项的**最高档位 ≥ 殿堂（RGQ_HALL）**；
始祖（`RGQ_PRIMORDIAL`）叠一层更长更亮的强化版。

| | 殿堂 | 始祖 |
|---|---|---|
| 时长 | 2.8 秒 | **4.2 秒** |
| 光扇外沿 | +180 | **+240** |
| 光环圈数 | 1 | **2** |
| 火花数 | 24 | **36** |
| 全屏彩幕 | 130 / 72 | **170 / 110** |
| 额外 | — | 同时改写背景 + 音乐（既有的用户要求） |

**四层演出**（按绘制顺序，全部画在世界层，与卡片共用 2x 坐标约定）：

| 层 | 内容 |
|---|---|
| ① `drawCelebBack` 卡片**之下** | 顶部光柱 + 18 道旋转光扇 + 外扩光环（点阵） |
| ② 卡片本身 | 原有翻牌逻辑**完全不动** |
| ③ `drawCelebFront` 卡片**之上** | 全屏彩幕强闪 + 卡缘脉冲 + 冲击波 + 迸射火花 |
| ④ `drawCelebText` 高分辨率文字层 | 档位横幅「殿 堂 降 临 / 始 祖 降 临」+ 卡名 + 副标题 |

设计要点：
- **所有效果都只依赖归一化进度 `p`（0→1）**，时间基准是 `gCelebT/gCelebDur`。
  于是测试可以把 `gCelebT` 摆到任意进度抓帧 —— 不必等实时演出、不依赖帧率，
  逐像素基线照样成立。
- 火花的**角度/速度/延迟全部由固定种子 `gCelebSeed` 决定** → 抓帧可复现，
  同一次演出形状稳定，不会每帧乱跳。
- 档位横幅用**字体的档位切换**（gF30 → gF72 → gF54）伪造"弹出过冲" ——
  GDI 不能缩放字体，这样最省事且不引入新资源。
- 半透明只能用 `fillRectA`（GDI 图元没有 alpha），所以闪光/彩幕都走它。

## 四、★ 过程中查出的两个真缺陷（都不是本轮引入的）

### 1. `createLayers()` 是半成品（上一轮 iOS 移植时抽出）

抽的时候把**世界变换的初始化留在了 WinMain 里**，于是任何不走 WinMain 的调用方
（headless 抓帧测试、以及 iOS 后端 —— 那边根本没有 WinMain）拿到的画布是 1:1 的，
所有绘制会缩到左上角四分之一。
→ 已把 `SetGraphicsMode(GM_ADVANCED)` + `gScaleXF/gIdentXF/gCurXF` 全部移进 `createLayers()`。
（`GM_ADVANCED` 也必须在那里设：默认的 `GM_COMPATIBLE` 下 `SetWorldTransform` 不生效。）

### 2. 抓帧测试把 `gWorldDC` 当 `render()` 的输出 DC 传了

`render(out)` 的**收尾会把画面呈现回 out**。把 `gWorldDC` 传进去 → 世界层被自己的
1x 缩略图覆盖，画面里出现**两套尺度、两个中心**（看起来像"重影"，我一度误判成变换污染）。
→ 正确做法：传一块**独立的临时 DC** 给 `render`，让呈现写到它上面，`gWorldDC` 保持干净再倒出。
已把这个坑写进 `_test_primordial_buff.c` 的注释。

> 这两个都属于"测出来的现象指向了错误的怀疑对象"。经验：
> **当画面出现"两套尺度"时，先怀疑输出 DC 被写回了同一个缓冲**，再去查变换。

## 五、验证

`_test_primordial_buff.exe`（**21 条断言 ALL PASS**）：

| 组 | 覆盖 |
|---|---|
| A 数值 | 始祖 ≥4000×、比殿堂强 ≥30×、攻速 ≥3.0、范围 ≥8.0、能力 64 条；**僵尸血量未被连带放大**且 <60× |
| B 威压 | 无始祖 0 掉血；有始祖 5.50%/s + 减速 + 存活计数正确；空场判定为 0 |
| C 庆典状态机 | 低档位不触发；殿堂触发且**不改写世界**；始祖触发、时长更长、**改写世界**；计时结束状态清干净 |
| D 抓帧 | 殿堂/始祖 × 4 个进度 = 8 张，进度可复现 |

**14 个回归测试全 PASS**；`pvz.c` 零告警；`pvz.exe` 已重编；真档 53 星 / 88 金币 / 91 株正常推进。
证据图：`art/panels_prim_celeb_primordial.png`（始祖）、`_hall.png`（殿堂）、`_flash.png`（开场闪光）。
